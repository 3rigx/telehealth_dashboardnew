# TeleRehab Dashboard — Work Log

Running log of work done and changes made. **Newest entries are appended at the bottom of the Change Log.**
Entry format:

```
### YYYY-MM-DD  HH:MM
**DO:**  <what was changed / done>
**Why:** <the reason / intent>
```

> Times for entries before 2026-07-02 are approximate (reconstructed; precise timestamps
> weren't recorded at the time). Entries from 2026-07-02 onward use real timestamps.

---

## Pending / Recommended fixes (prioritized)

Derived from the 2026-07-02 critical review.

**P0 — data integrity / scientific validity** ✅ DONE 2026-07-02 (Change Log 14:06)
- ~~Measure Flutter↔Unity latency & correct marker times.~~ Ping/pong handshake added; offset measured & stored in `run.json`; marker `t_ms` corrected onto Unity's record clock (raw kept as `t_run_ms`). **Needs one live run to confirm the measured offset is sane.**
- ~~Recompute reps from the recorded file.~~ Reps recomputed from `zed_skeleton.json` per active window; canonical value in `run.json` (`repsSource` / `liveReps` / `repsByBlock`).
- ~~Persist markers incrementally.~~ Markers appended (sync + flush) to `_pending/…/markers.partial.csv` as they fire; reconciled into the session folder on save.
- ~~Calibrate the EEG µV scale.~~ Scale made runtime-overridable (PlayerPrefs `EegScaleUv`) + calibration procedure documented in-code. **The numeric calibration still needs a paired headset+Recorder capture to finalise.**

**P1 — reliability**
- ~~Pre-flight signal-quality gate before Begin~~ ✅ DONE 2026-07-02 14:21 (Change Log) — EEG-railed / FSR-zero / skeleton-tracked check gates Begin, with an operator override.
- **Write absolute timestamps from Unity** and drop the delta + `cumulativeTimeline` heuristic in `session_repository.dart`.
- **Persist dropped-frame counts** (EEG `FramesDropped`, and skeleton/FSR gaps) into the manifest so analysis sees gaps.
- **Add tests**: EEG decode (known frame→µV), sequence generator (even split / no-repeat / counterbalance counts), timeline reconstruction, CSV parsers, rep detector.
- **Guard against FSR + EEG sharing a COM port** — Settings currently allows `fsrUsbPort == eegComPort`. When both point at the EEG's Bluetooth port, the FSR opens it first and the EEG then fails with "Access is denied" (silently → "continuing without EEG"). Warn/block in the picker. *Diagnosed 2026-07-27 from Player.log — this was the real cause of "EEG connects in live but not the protocol builder": the protocol had FSR on, on the same COM7 as the EEG.*
- **Fix repeated session re-drive hanging sensor init** — back-to-back `configure_session`+`start_session` without returning to the menu reloads the capture scene and re-inits the ZED without releasing the previous instance; after ~3 re-drives with no camera, sensor init hangs (the 20 s "previewing anyway" fallback stops firing). Reachable if a clinician starts a new dashboard session without Close. *Found 2026-08-05 during perf testing; verify severity with a real camera attached.*

**P2 — product / research value**
- **Auto-epoched, analysis-ready export** (per-condition long-format CSV / EDF) instead of raw zips.
- **Study-level counterbalance manager** (Latin-square group assignment across the participant pool) — current per-run random seed doesn't balance the group.
- **Trial annotation + exclude flag** ("participant sneezed at block 7").
- **Aggregate analysis** across the protocol's own block structure (condition A vs B).
- **Shader-compilation jank (cosmetic)** — Skia compiles the gradient/glow paints (EEG blobs, foot/plantar heatmaps, neon avatar) on first use → a transient first-render stutter. Impeller (the clean fix) black-screens on this Windows GPU in Flutter 3.44, so it's deferred; re-try on a newer Flutter / D3D backend, or bundle SkSL warm-up (`--cache-sksl` → `--bundle-sksl-path`). *Accepted as-is 2026-08-05 — cosmetic; app is otherwise smooth (stable CPU, flat memory, ~79 fps).*
- **Performance testing kit** — `tool/ws_perf_test.dart`, `tool/sample_resources.ps1`, `PERF_TESTING.md`. Run before releases to catch pipeline/UI/endurance regressions. *Added 2026-08-05.*

---

## Change Log

### 2026-06-18 → 2026-06-19  (time approx)
**DO:** Built the license-free Unicorn Hybrid Black **EEG capture pipeline** — `UnicornEegConnector` (bg-thread raw-SPP decode, counter-locked framing) + `UnicornEegProbe`, `EegRecorder`, `EegBandPower` (Goertzel θ/α/β), `EegCsvWriter`→`eeg.csv`; wired into `ExerciseController` gated by `SessionContext.EegEnabled`; broadcaster emits an `eeg` object; Flutter `EegData` + `eeg_panel`/fusion + replay loader. Confirmed the 45-byte frame layout (2 header + 8×3 EEG 24-bit **big-endian** + accel + gyro + battery + uint32 LE counter + `0D 0A`) byte-by-byte against a real capture.
**Why:** Add EEG as a third modality without a g.tec licence (`UnicornSuite.lic` was empty) by reverse-engineering the raw Bluetooth-SPP protocol.

### 2026-06-22  (time approx)
**DO:** Validated the headset in **Unicorn Recorder** — clean channels + a clear eyes-closed **alpha** rise on the occipital channels. Established that the unworn bench/touch test is inherently ambiguous (open electrodes rail); the decode itself is correct.
**Why:** Prove the hardware + our decode read genuine EEG before trusting the pipeline.

### 2026-06-25  (time approx)
**DO:** Locked Protocol Builder decisions (one continuous recording + block markers; single-window participant toggle; per-class exercise/feedback; permuted-blocks counterbalance; ≈6-min editable timing). Built **Phase 1** — `models/protocol.dart`, `services/protocol_repository.dart` (JSON persistence), `screens/protocol/protocol_builder_screen.dart`, nav card + `/protocols` route, `AppSettings.protocolsRoot`.
**Why:** Turn ad-hoc capture sessions into a reusable, research-grade experiment designer.

### 2026-06-26  (time approx)
**DO:** **Phase 2** — `protocol_runner.dart` (clock/sequencing + knee-angle rep detector), `participant_screen.dart` (phased full-screen run view + feedback widgets), `widgets/eeg_channel_map.dart`. **Phase 3** — `protocol_run_controller.dart` (live run: connect→configure→start_session→Begin→`start_recording`→`stop_recording`; writes `markers.csv`/`protocol.json`/`run.json` on `session_saved`); "Run Live" entry. **Phase 4** — marker parsing + replay block-timeline overlay + `.zip` export (`archive` pkg). Moved block timing **onto each class** (`ProtocolClass`). Added warm-up detection (`UnityConnectionService.receivingLive`) + `SensorWarmupLoader` gating the live monitor and Run Live "Begin". Made the **back button / dispose stop Unity recording** on the live monitor and participant screens. Run Live now force-disables dashboard mocks (live = real data).
**Why:** Complete the builder end-to-end; make sensor warm-up an explicit visible state; never leave Unity recording orphaned; ensure a "live" run shows real sensors, not mock overlay.

### 2026-06-29  (time approx)
**DO:** Restyled the whole app to the **Daydream design system** — new `AppColors` palette (Peach/Sky/Ink/Paper + pastels + Violet interactive), `AppRadius`/`AppSpace`, Schibsted Grotesk text theme + Caveat eyebrows; swapped Inter→Schibsted Grotesk app-wide; **light "chrome"** (Home/Builder/Settings) + **dark "Ink" data screens** (live monitor/replay/participant) using `ink*` tokens; global **pill buttons** via theme; per-screen editorial polish. Added a **runtime light/dark toggle** (mode-aware semantic tokens, persisted `AppSettings.darkMode`, sun/moon button in the Home rail). Added the **active-phase exercise-guide GIF** panel, **runtime hide toggles for the FSR/EEG panels** in the participant control strip, and a **gamified rep counter** (`_RepGauge`: progress ring per 5 reps, pop animation, flame streak, milestone cheers).
**Why:** Adopt the provided brand/design system; give day/night flexibility; let the participant follow the exercise guide during the block; let the operator declutter the run view; motivate the participant.

### 2026-07-02  09:50
**DO:** Delivered a critical system review (clock desync between Flutter markers and Unity data; uncalibrated EEG µV; cross-modal alignment risk; reps computed from the live/mock stream; markers as a single point of failure; per-run vs study-level counterbalancing; no tests). Created this `WORKLOG.md` and backfilled the change log, plus the prioritized fix backlog above.
**Why:** User asked for an honest, critical assessment and a maintained task log (date / time / DO / Why).

### 2026-07-02  14:06
**DO:** Implemented all four **P0 data-integrity fixes**.
- **Reps from the recorded file** — new `lib/services/rep_analysis.dart`: shared `KneeRepCounter` (single source of truth for the hysteresis, now used by both the live runner and the recompute), `kneeFlexionDeg` (from hip→knee→ankle joints), and `recomputeRepsFromRecording` (parses `zed_skeleton.json`, rebuilds the timeline via the now-public `cumulativeTimeline`, counts reps per active window). `ProtocolRunController._writeArtifacts` recomputes and writes canonical `reps` + `repsSource`/`liveReps`/`repsByBlock` into `run.json`. `ProtocolRunner` refactored to use the shared counter.
- **Incremental markers** — `ProtocolRunner.onMarker` callback fires as each marker is logged; the controller appends it (sync + flush) to `<protocolsRoot>/_pending/<pid>_<ts>/markers.partial.csv` with a `run.partial.json` sidecar, and reconciles/cleans it into the session folder on save. Crash mid-run no longer orphans the block structure.
- **Clock offset** — Flutter sends 5 `ping`s at run start, keeps the lowest-RTT `pong`, computes `clockOffsetMs = (sentAt + rtt/2) − unityRecMs`; Unity (`TelerehabBroadcaster.RecordingClockMs` + a `ping`→`pong` case in `TelerehabWebSocketServer`) echoes its record-clock ms on the main thread. `markers.csv` now writes corrected `t_ms` (+ raw `t_run_ms`), and `run.json` records `clockOffsetMs`/`clockRttMs`/`markerTimebase`. Degrades gracefully to the raw run clock if Unity doesn't answer.
- **EEG scale** — `UnicornEegConnector.EegScaleUv` is now a runtime-settable field seeded from PlayerPrefs `EegScaleUv` (default = derived `DefaultScaleUv`), with the full calibration procedure documented in-code.
**Why:** Make the recorded data trustworthy for research — reps reflect saved data not the lossy/mock display; block markers survive a crash; markers align to the sensor timeline; absolute EEG µV can be calibrated without a recompile.
**Verify:** `flutter analyze` clean on all changed Dart (0 new issues; 15 pre-existing info lints elsewhere). Unity C# is additive (static property, one DTO field + `case`, one static field) but the MCP bridge was revoked so it wasn't validated through Unity — needs a normal Editor recompile + one live run to confirm the pong offset and the recorded-rep count.

### 2026-07-02  14:21
**DO:** Added the **P1 pre-flight signal-quality gate**. New `lib/services/signal_quality.dart` (`assessSignalQuality` → `SignalReport` of per-sensor `SignalLevel` ok/warn/bad, only for the sensors the protocol enables): ZED = a body with ≥8 tracked joints; FSR = non-zero load (catches the wrong-COM-port all-zeros case); EEG = ≥6/8 channels within [0.5, 5000] µV RMS (railed = open electrode, flat = dead). `ProtocolRunController` exposes `signalReport` + `canBegin` (warm-up done AND signals pass, unless overridden) + `overrideQualityGate`/`setOverrideGate`. The participant `_beginOverlay` now shows a live per-sensor checklist, disables **Begin** until green, and offers a "Start anyway — I've checked the sensors" override (button then reads "Begin anyway").
**Why:** Stop wasting whole sessions on a mis-connected sensor — the FSR-zero (wrong port) and unworn-EEG (railed) failures we already hit are now visible and blocking *before* recording starts, while still letting the operator proceed deliberately.
**Verify:** `flutter analyze` clean on the 3 changed/added files (0 new issues). Pure `assessSignalQuality` is unit-testable; live checklist refreshes on each `sensor_update` (screen already rebuilds on the connection listener).

### 2026-07-03  08:46
**DO:** Fixed a Unity bug that blocked ZED-only capture (surfaced testing the P0 work with just the ZED 2i). `SensorSystemController.Start()` (Unity C#) always created + opened the FSR serial port regardless of `SessionContext.FsrEnabled`, and `FsrConnector.Connect()` was unguarded — with no FSR plugged in it threw `IOException: The port 'COM3' does not exist.`, aborting `Start()` so `_fsrController` stayed null and `Read()`/`GetState()` then NPE'd every frame, taking the ZED capture down with it. Now: (1) skip FSR entirely when `SessionContext.FsrEnabled` is false; (2) wrap the connect so a hardware/port failure logs a warning and continues *without* FSR instead of aborting; (3) null-guard `Read()` and emit empty `FSRState` from `GetState()` when there's no FSR. `SessionWriter` already gates `fsr.csv` on `FsrEnabled`, so a ZED-only run writes no bogus FSR data.
**Why:** Let a camera-only protocol (FSR/EEG disabled) actually run, and make the sensor system robust to any one sensor failing to connect (the ZED should never die because an insole is unplugged).
**Verify:** Not validated through Unity (MCP bridge still revoked) — needs an Editor recompile. Change is small/scoped: gate on the existing `SessionContext.FsrEnabled`, catch around `Connect()`, `_fsrController?.` + empty `FSRState` fallback (types confirmed: `CombinedFSRController.GetState()`→`(FSRState,FSRState)`, `FSRState()` no-arg ctor). Expect the COM3 error gone and the ZED to start for a ZED-only run.
**Result:** ✅ ZED-only run succeeded (session `treyuyy59/2026-07-03_08-53-34_Idle`). P0 pipeline confirmed end-to-end: `repsSource="recorded_skeleton"`, `reps=33` (per-block breakdown correct — REST blocks 0, MOVE/MOVE+COUNT 3–5 each), `markers.csv` has the 7-col schema + `t_run_ms`, `markerTimebase="unity_record_clock"`. Two findings → next entry.

### 2026-07-03  10:01
**DO:** Two fixes from the first live ZED-only run's data.
- **Live rep counter was counting 0 on real data** (`run.json liveReps=0` vs recomputed `reps=33`). Root cause: the live path read the `jointAngles` stream, which is the *interior* knee angle (180°=straight), but the hysteresis thresholds expect *flexion* (0°=straight) — so it never tripped. Changed `ProtocolRunner._kneeAngle` to compute `kneeFlexionDeg(skeleton)` (the same geometry the recorded recompute uses), so the live gamified count is correct and matches the saved value. (This run also *validated* Fix #1: recompute recovered 33 where the live count would have saved 0.)
- **Clock offset was noisy/over-measured** (`clockOffsetMs=1008`, `clockRttMs=455` → first marker `t_ms=-1008`). The 5 pings at 150 ms all landed in the busy window right after recording start (scene + first frames saturate Unity's main thread), inflating RTT and biasing the offset. Changed `_startClockSync` to ping every 400 ms for up to ~12 s (30 pings) keeping the lowest-RTT reply, stopping early once a clean (<15 ms) round trip lands — so the offset is measured during a quiet moment and is trustworthy.
**Why:** Make the participant-facing count correct (not just the saved file), and get an accurate clock offset instead of one biased by main-thread contention.
**Verify:** `flutter analyze` clean on both changed files. Needs a re-run to confirm: `liveReps` should now ≈ `reps`, and `clockOffsetMs` should drop to a low/steady value (if it stays ~1 s with a clean <15 ms RTT, that's a genuine start_recording→record-clock latency to address at the source next).

### 2026-07-03  12:05
**DO:** **Phase 1 of the generalized rep engine** (see memory [[rep-counting-and-authoring]]). Replaced the single hardwired knee detector with a person-calibrated, joint-agnostic engine. New `lib/services/motion_rep.dart`: `channelAngle` (interior flexion of knee/elbow/shoulder/hip × L/R from joint positions); `CyclicRepDetector` — counts oscillations relative to the individual's OWN observed range (Schmitt trigger at 70%/30% of range, mean≈rest, `minRangeDeg=12` noise floor, `debounceMs=350`), so limited ROM still counts (rep = effort, not achievement); `analyzeBlockReps` runs a detector per channel, merges events across channels within `mergeMs=300` (one physical movement / synchronized limbs = 1 rep; alternating limbs = separate) and reports the dominant joint; `LiveRepEngine` = the same logic online. `rep_analysis.dart` recompute now uses `analyzeBlockReps` and returns per-rep events + dominant joint per block. `protocol_runner` live path swapped to `LiveRepEngine` (feeds the whole skeleton). `run.json` gains `repJointByBlock` + `repEvents` (per-rep timestamps, for audit). Removed `KneeRepCounter`/`kneeFlexionDeg`.
**Why:** Fix "only one leg counts" (it was locked to `kneeR`, which renders as the participant's left leg) and make the counter work for ANY exercise and any tracked limb — the foundation the authoring flow (Phase 2) reuses to derive a movement's signature. Person-calibration is the equity fix: no absolute angle bar that would penalise limited-ROM users.
**Verify:** `flutter analyze` clean on all 4 changed files; no remaining refs to the removed detector. Needs a ZED-only re-run to confirm: raising EITHER leg (or an arm) now counts; `run.json` should show `repJointByBlock` (e.g. "knee") + a `repEvents` list whose per-block counts match `repsByBlock`. Tunables (`minRangeDeg`/`mergeMs`/`debounceMs`) may need adjusting against real footage.
**SUPERSEDED same day — see 12:27.**

### 2026-07-03  12:27
**DO:** **Removed the rep counter / score entirely** (reverses the 12:05 Phase 1 work). Per the user: a completion score is subjective and inequitable — it assumes everyone can complete the movement, penalising limited-ROM/disabled participants — so counting is dropped, not made person-relative. Deleted `motion_rep.dart` + `rep_analysis.dart`; stripped `reps`/`_repsAccum`/`totalReps`/`feedSensor`/`activeWindowsFromMarkers` from `ProtocolRunner`; removed the rep recompute + all rep fields (`reps`/`totalReps`/`repsSource`/`liveReps`/`repsByBlock`/`repJointByBlock`/`repEvents`) from `run.json` in `protocol_run_controller`; removed `_RepGauge` + `_repCard` + the "total reps" stat + the `feedSensor` call from `participant_screen`; removed `ClassFeedback.repCounter` (model + `toJson`/`fromJson`) and the "Repetition Counter" builder toggle + template defaults. The raw skeleton is still recorded; the app simply never counts/scores reps. `run.json` keeps sequence + clock-alignment; `markers.csv` unchanged.
**Why:** User: "remove the count/rep score because it's subjective to the fact everyone can complete the exercise." Equity-first — no success metric that a participant can "fail".
**Note:** A SEPARATE, older single-session **replay analytics** rep detector still exists (`replay_engine._detectReps` → `replay_screen` "Repetitions" + ROM charts). Flagged to the user; left in place pending their decision (it also carries descriptive ROM data).
**Verify:** `flutter analyze` clean (0 errors; the usual 15 pre-existing info lints). Next: build the **Exercise authoring** section (record a movement via ZED → native skeleton-avatar guide, selectable in the protocol builder) — see memory [[rep-counting-and-authoring]].

### 2026-07-03  12:40
**DO:** **Exercise authoring — Increment 1 (library foundation).** New `Exercises` section: `models/exercise_asset.dart` (`ExerciseAsset` — recorded avatar clip or uploaded media, stored per-folder), `services/exercise_repository.dart` (`<exercisesRoot>/<id>/exercise.json` + `skeleton.json`; `importFromSkeletonJson`, `loadFrames` reusing `skeletonFromUnityJson`+`cumulativeTimeline`, save/delete), `widgets/avatar_guide_player.dart` (loops recorded skeleton frames on the existing `Skeleton3DView` bone avatar at the clip's own rate), `screens/exercises/exercise_library_screen.dart` (grid of exercises, preview dialog with the looping avatar, delete, "New from recording" = pick a `zed_skeleton.json` → name → import). Added `AppSettings.exercisesRoot` (+ default `…/Exercises`), the `ExerciseRepository` provider + `/exercises` route in `main.dart`, and an "Exercises" nav card in the Home rail.
**Why:** Stand up the authoring library so exercises become reusable avatar guides. Import-from-recording works TODAY with any existing `zed_skeleton.json` (e.g. the treyuyy59 session) — no Unity change — proving the model + avatar playback before building live capture.
**Verify:** `flutter analyze` clean (0 errors; usual 15 info lints). Test: Home → Exercises → New from recording → pick a session's `zed_skeleton.json` → name it → Preview should loop the movement on the avatar. Next: Increment 2 = record directly via the ZED (reuse the capture pipeline); Increment 3 = pick an exercise per class in the builder + play it as the participant's block guide.

### 2026-07-03  12:44
**DO:** **Exercise authoring — Increment 2 (record with the ZED).** `services/exercise_record_controller.dart` — `ExerciseRecordController` reuses the capture pipeline for a ZED-only authoring recording (mocks off; configure `patientId:_exercises`, zed on / fsr+eeg off → never touches COM3; start_session → warm-up gate on `receivingLive` w/ 25 s fallback → start_recording → stop_recording → on `session_saved` calls `ExerciseRepository.importFromSkeletonJson` on the session's `zed_skeleton.json`; errors if 0 frames). `screens/exercises/exercise_record_screen.dart` — dark full-screen flow: warm-up loader → live `Skeleton3DView` preview + **Record** (red) → recording w/ pulsing dot + elapsed timer + **Stop & save** (green) → saving → done ("Back to library") / error. Library header + empty state now offer **Record with ZED** (primary) alongside **Import** (a `zed_skeleton.json` file); `_recordNew` prompts a name then pushes the record screen.
**Why:** The core of the feature — a clinician demonstrates a movement once and it becomes a reusable avatar guide, no filming/GIF needed.
**Verify:** `flutter analyze` clean on the 3 changed/new files (0 issues). Needs Unity running + the ZED to test live (recording reuses the same warm-up/`session_saved` path already validated). Next: Increment 3 — builder picker (`ProtocolClass.exerciseId`) + participant block guide plays the avatar via `AvatarGuidePlayer`.

### 2026-07-06  11:40
**DO:** **Exercise authoring — Increment 3 (use it in protocols).** Added `ProtocolClass.exerciseId` (+ toJson/fromJson; `exerciseType` now the display name set on pick). `AvatarExerciseGuide` widget (in `avatar_guide_player.dart`) loads an authored exercise's frames once by id and plays them on the bone avatar. Protocol builder class editor gains an **"Exercise guide (avatar)"** picker (`_pickExercise` → SimpleDialog of the library → sets `exerciseId`+`exerciseType`; clearable). Participant screen block guide now prefers the avatar: active view + instruction lead-in show `AvatarExerciseGuide(exerciseId)` when set, else fall back to the uploaded GIF (`_guidePlayer`/`_refAnimation`). Feature is now end-to-end.
**Why:** Close the loop — an authored (or imported) exercise is selectable per class and plays as the participant's block guide, replacing the need to film/upload a GIF.
**Verify:** `flutter analyze` clean (0 errors; usual 15 pre-existing info lints). End-to-end test (no Unity needed for the import path): Exercises → Import a `zed_skeleton.json` → Protocol Builder → a class → Exercise guide → Choose it → Rehearse/Run → the block shows the looping avatar guide. Live path (record-with-ZED) needs Unity+ZED.

### 2026-07-06  12:30
**DO:** **Avatar look — solid mannequin instead of a line stickman.** Rewrote the render loop in `widgets/skeleton_3d_view.dart` (`_Skeleton3DPainter`): bones now draw as tapered, depth-shaded **capsules** (perspective-correct radius per end from a new `_girth` world-space thickness map — bulky torso/head tapering to thin wrists/ankles — with rounded caps + a soft centre sheen for tube volume), and joints as **lit spheres** (radial gradient, light from upper-left). Same recorded joint data, same orbit/zoom/depth-sort; the active limb keeps its green/orange target colour + ring. No new dependencies. Affects every place the avatar shows (exercise preview, block guide, Movement Mirror, replay).
**Why:** User asked if the recorded exercise could be "converted to avatar instead of the line stickman." Chose the solid-mannequin route (over a skinned glTF humanoid in Flutter or a Unity avatar) — it reads as a real body, costs almost nothing, keeps all recordings valid, and needs no 3D engine / motion retargeting.
**Verify:** `flutter analyze lib/widgets/skeleton_3d_view.dart` → No issues found. Visual: Exercises → preview any exercise (or the Movement Mirror) → figure is now a solid, rounded body. `_girth` values are easy to tune if a body part looks too thick/thin.

### 2026-07-06  13:05
**DO:** **Selectable avatar styles (Settings) — the mannequin is now one option, not forced.** User didn't like the solid mannequin, so made the figure look a user setting with 4 variations. New `models/avatar_style.dart` (`enum AvatarStyle { lines, mannequin, neon, blocky }` + label/description/token/`fromToken`). `AppSettings.avatarStyle` persisted (`avatar_style` pref, **default `lines`** = the original clean stickman) + `setAvatarStyle`. `Skeleton3DView` gained an optional `style` param (falls back to `context.watch<AppSettings>().avatarStyle`); `_Skeleton3DPainter` refactored so `paint()` dispatches `switch (style)` into four self-contained renderers — `_styleLines` (original thin bones + dots), `_styleMannequin` (the capsules+spheres from 12:30), `_styleNeon` (blurred glow tubes + hot-core joints via `MaskFilter.blur`), `_styleBlocky` (flat untapered rectangular segments + faceted shading + rounded-box joints). Shared `_sortedBones`/`_sortedJoints`/`_axis` helpers. Floor grid, target ghost and angle arc are style-independent. Settings gains a **MOVEMENT AVATAR** section: a live 220px `Skeleton3DView` preview (seated demo) + radio-style `_avatarChoice` rows; changing it live-updates every avatar in the app.
**Why:** The "best" figure is subjective — a setting lets the clinician pick (and revert to the plain lines they had). All styles are pure-painter variations of the same recorded joints, so zero data/processing cost and no new deps.
**Verify:** `flutter analyze` on the 4 changed/new files → No issues found. Visual: Settings → Movement Avatar → tap Lines / Mannequin / Neon / Blocky → preview + all avatars switch instantly; choice persists across restarts (default = Lines).

### 2026-07-06  13:30
**DO:** **5th avatar style — CARTOON (anime-inspired chibi).** User asked for an anime look; chose the cheap painter route (over a real Unity/Flutter VRM character, which was explained as a separate big project). Added `AvatarStyle.cartoon` (+ label/description/token/`fromToken`) and `_styleCartoon` in `skeleton_3d_view.dart`: outlined, cel-shaded chunky limb capsules (torso bones = teal "clothes", limbs = skin tone, active limb still highlights) drawn with a "bigger dark capsule behind smaller fill" ink outline + one flat cel-shadow facet; then an oversized billboarded head sitting above the neck — violet hair back-blob, skin face with lower-face shadow, hair bang tufts, big anime eyes (white + violet iris + highlight + upper-lash arc), pink cheek blush, and a small smile arc. New `_capsulePath` helper shared with the outline passes. Face always billboards to the camera (no head-orientation data). Shows up automatically in Settings → Movement Avatar (the section iterates `AvatarStyle.values`).
**Why:** Wanted an anime avatar without the cost/architecture change of a rigged VRM character. This is a stylised cartoon mannequin (no expressions/hair-physics/clothing detail) — set expectations accordingly; a true VRM character remains a future option (Unity + UniVRM + toon shader + ZED avatar sample).
**Verify:** `flutter analyze lib/widgets/skeleton_3d_view.dart lib/models/avatar_style.dart` → No issues found. Visual: Settings → Movement Avatar → **Cartoon** → preview shows a big-headed anime-ish figure; try it on a real recording. Face/hair/limb proportions are all easy constant tweaks in `_styleCartoon` (e.g. head `2.15`, limb `1.28`, colours) if it needs adjusting.

### 2026-07-07  11:05
**DO:** **Participant privacy / ethics (P: IRB/GDPR gap).** Pseudonymity is now enforced by design. (1) **Registry**: `models/participant.dart` (`Participant`: code, enrolledUtc, consentVersion/consentUtc, status active/withdrawn/completed, clinical notes — deliberately NO name/contact/DOB fields) + `services/participant_repository.dart` (single `participants.json` under new `AppSettings.participantsRoot`; auto-issues codes P-001, P-002…; codes never reused). (2) **Strict enforcement**: `widgets/participant_picker.dart` — `pickParticipant()` (list of enrolled ACTIVE codes only, zero free-text) + `enrolParticipantDialog()` (Enrol disabled until "info sheet provided" + "written consent obtained" checked; consent version recorded; code issued on enrol). Home `SessionSetupPanel` patient TextField + folder-name chips REPLACED by the picker tile; `_start` re-validates the code against the registry. Protocol builder `_askParticipantId` free-text dialog DELETED → `pickParticipant`. lastPatientId prefills only if still an enrolled active code. (3) **Participants screen** (`screens/participants/participants_screen.dart`, route `/participants`, home nav card): enrol, status chips, mark completed / withdraw, **Delete recorded data** (right-to-erasure: removes `<sessionsRoot>/<code>` after explicit confirm), remove registry entry, and **Export de-identified data**. (4) **Export**: `services/deidentified_export.dart` — zips a participant's sessions with calendar dates → study days (`day004_s01_Motion`; ISO timestamps inside JSON → `day004T14:22:05`), `notes` keys stripped from all JSON, optional re-alias (P-003 → EXP-…), manifest.json + README data dictionary; CSVs (relative ms) copied as-is; unparsable JSON is omitted rather than passed through unscrubbed. (5) **Settings → DATA PROTECTION**: BitLocker status of the sessions drive via the Shell COM property `System.Volume.BitLockerProtection` (works without admin; verified returns 1=ON on this machine) + guidance text.
**Why:** IDs were free text — a typed real name became a folder name and lived in every JSON forever. Decisions (user): identity map lives OUTSIDE the software (paper/PI file — the app can't leak what it never holds); enforcement is STRICT (picker only). Raw files stay plaintext (Unity writes them) → at-rest story = pseudonymous codes + full-disk encryption, now visible in Settings.
**Verify:** `flutter analyze` on all 10 changed/new files → No issues found. BitLocker probe tested standalone → returns 1 (ON). Manual test: Home → Participants → Enrol (checkboxes gate the button) → code P-001 issued → Live Exercise setup now shows a picker (no typing) → run a session → folder named P-001 → Participants ⋮ → Export de-identified → zip has day###_s## folders, no notes, no calendar dates. NOTE: legacy free-text session folders (e.g. treyuyy59) stay on disk and are no longer selectable for new sessions; replay/library can still open them. Rename/adopt tooling deliberately deferred.

### 2026-07-07  11:35
**DO:** **Gender demographic at enrolment.** `ParticipantGender` enum (`unspecified/female/male/nonBinary/notDisclosed` + token/label/`fromToken`) on the `Participant` model (default `unspecified`; older `participants.json` entries load fine). Enrolment dialog gains optional choice chips (Female / Male / Non-binary / Prefer not to say — tap again to clear); `ParticipantRepository.enrol` takes `gender`. Registry card subtitle shows it when set. De-identified export `manifest.json` includes `gender`.
**Why:** User asked for a gender input. Designed as a structured enum, NOT free text, so nothing identifying can be typed into it; gender is not a HIPAA safe-harbor identifier and is a standard analysis covariate — so unlike free-text `notes` (stripped) it is intentionally KEPT in the de-identified export.
**Verify:** `flutter analyze` on the 5 changed files → No issues found. Manual: Enrol → pick a chip → card shows e.g. "· Female"; export a participant → manifest.json has `"gender": "female"`.

### 2026-07-07  12:10
**DO:** **First-run onboarding / setup guide.** New `screens/onboarding/onboarding_screen.dart` (route `/onboarding`): a 5-page PageView wizard — (1) Welcome: what the system does (ZED motion capture, multi-sensor recording, protocols) + research-use-only disclaimer; (2) What you need: Windows PC + disk + BitLocker, the Smart Game Unity build (exe path in Settings, ws://localhost:8765), ZED 2i + ZED SDK + NVIDIA/CUDA GPU + camera placement, FSR insoles (OPTIONAL badge), Unicorn EEG (OPTIONAL), and mock-data mode for no-hardware exploration; (3) Privacy first: enrol-with-consent → code issued, codes-only/identity-log-off-machine rule, de-identified export, BitLocker check; (4) Running a study: numbered steps Enrol → Record exercise guides → Design protocol → Run live (signal gate) → Replay/export, plus Live Exercise quick capture; (5) Good to know: Unity status colours, don't-override-the-signal-check advice, avatar styles, data folder locations, how to reopen the guide. Dots + Back/Next/Skip/"Get started". `AppSettings.onboardingSeen` (pref `onboarding_seen`) + `setOnboardingSeen`; HomeScreen post-frame pushes `/onboarding` once when unseen (settings are loaded before runApp, so no race); Settings gains a **HELP** section with "Show setup guide".
**Why:** User asked for an onboarding guide on first install explaining what to do, how, and the install/use requirements. Content is grounded in the real system (sensor set, Unity bridge, privacy workflow, signal gate) rather than generic copy.
**Verify:** `flutter analyze` on the 5 changed/new files → No issues found. Manual: delete the `onboarding_seen` pref (or fresh install) → app opens the guide once; Skip/Get started persists the flag; Settings → Help → Show setup guide reopens it.

### 2026-07-07  12:45
**DO:** **Windows installer pipeline (Inno Setup).** New `installer/` folder: `telerehab.iss` (Inno Setup 6 script — installs to `Program Files\TeleRehab` with `dashboard\` + `unity\` subfolders; Unity player is an optional component compiled in only when `installer\unity_payload` exists (`#if DirExists` guard); bundles + silently runs `vc_redist.x64.exe` when present; Start-menu/desktop shortcuts; stable `AppId` for clean upgrades; final page reminds about the ZED SDK), `build_installer.ps1` (one-click: `flutter build windows --release` → copy `-UnityBuild <folder>` into `unity_payload` (robocopy, excludes `*_BackUpThisFolder…` symbols) → auto-download VC++ redist from aka.ms → locate ISCC → compile → `installer\Output\TeleRehabSetup-1.0.0.exe`), and `README.md` (prereqs incl. making the Unity player build via File→Build Settings; what the installer deliberately does NOT install: ZED SDK, sensor drivers). `.gitignore` +`installer/Output|unity_payload|redist`. **Flutter change:** `UnityLaunchService.init()` now auto-detects a bundled Unity player — if the saved exe path is empty/stale it probes `<dashboard exe>\..\..\unity\*.exe` (skipping `*CrashHandler*`) and persists it, so a bundled install is zero-config. Decision (user): bundle **dashboard + Unity build** in one setup exe.
**Why:** User asked how to create a Windows installation file. Inno Setup chosen over MSIX (sideloading MSIX needs a trusted signing cert on every target PC — friction for lab rigs) and MSI/WiX (overkill).
**Verify:** `flutter analyze` unity_launch_service.dart → No issues found. `flutter build windows --release` kicked off to validate the payload builds. NOTE: Inno Setup not yet installed on this machine (`winget install JRSoftware.InnoSetup`) and no Unity player build exists yet (project has only run in-editor) — both are one-time steps documented in installer/README.md.

### 2026-07-07  13:20
**DO:** **Installer pipeline validated end-to-end → `installer\Output\TeleRehabSetup-1.0.0.exe` (33.8 MB, dashboard-only).** Fixes along the way: (1) `flutter build windows --release` succeeded (32.1 MB payload, ~60 s). (2) Installed Inno Setup 6.7.3 via winget (lands in `%LOCALAPPDATA%\Programs\Inno Setup 6` — already in the script's search list). (3) **This machine's Group Policy enforces `AllSigned` for PowerShell** (MachinePolicy scope — `-ExecutionPolicy Bypass` is ignored), so `build_installer.ps1` could never run → **ported to `build_installer.cmd`** (batch is exempt from PS policy) and deleted the .ps1. (4) First .cmd run looped garbage (`'al' is not recognized`, 9 MB of output): the file had **LF line endings — batch requires CRLF** (cmd mis-tokenizes `call`→`al`); converted to CRLF + pure ASCII. (5) `nobuild` now accepted as either argument (PowerShell swallows a bare `""` arg). (6) `ArchitecturesAllowed` x64 → `x64compatible` (Inno 6.7 deprecation). VC++ redist (24.4 MB) auto-downloaded into the bundle.
**Why:** Prove the whole chain works before the user needs it, and record the two machine-specific traps (GPO AllSigned → use .cmd; batch needs CRLF) so they aren't rediscovered.
**Verify:** `cmd /c installer\build_installer.cmd nobuild` → "Successful compile (16.8 s)" → `TeleRehabSetup-1.0.0.exe` 33.8 MB in installer\Output. Remaining for the full bundle: make the Unity player build (File → Build Settings → Windows) and rerun with that folder as arg 1.

### 2026-07-07  13:55
**DO:** **Full-bundle installer shipped: `installer\Output\TeleRehabSetup-1.0.0.exe` (88 MB) — dashboard + fresh Unity player + VC++ redist.** Found two STALE Smart Game builds on disk (Downloads\ 12-Jun, Desktop\Testing\ 22-May) — both predate the FSR-COM3 crash fix (03-Jul) and the ping/pong clock sync, so neither was bundled. Built a fresh player headlessly: `Unity.exe -batchmode -quit -projectPath telehealth-unity -buildWindows64Player "Builds\Windows\Smart Game.exe"` (6000.4.0f1; scene list Menu/Replay/CompleteExercise already correct; ~5 min; first exe-missing check was a race with the postprocess copy — the build had succeeded). Added `*_BurstDebugInformation_DoNotShip` to the robocopy /XD exclusions alongside the BackUpThisFolder one. `ArchitecturesAllowed/InstallIn64BitMode` → `x64compatible` (deprecation fixed).
**Why:** Complete the user's "where's the unity exe" → the honest answer was "nowhere current"; bundling a stale build would have shipped the exact crashes fixed this month.
**Verify:** `installer\build_installer.cmd "C:\UnityProjects\telehealth-unity\Builds\Windows" nobuild` → "Successful compile (60.1 s)" → 88 MB setup exe with the unity component present in [Components]. Fresh player lives at `telehealth-unity\Builds\Windows\Smart Game.exe`. NOT yet smoke-tested: running the setup exe on a clean machine and a bundled-install capture session (Unity player + ZED on the target).

### 2026-07-07  14:40
**DO:** **Fix: "Run live" (and exercise recording) never launched Unity.** User installed TeleRehabSetup, hit Run live → "Unity is not running" because `ProtocolRunController.prepare()` and `ExerciseRecordController.prepare()` only did `conn.connect()` and errored out — only the home-screen Live Exercise flow ever called `UnityLaunchService.launch()`. Never noticed in dev because the Editor was always in Play mode. Added shared `ensureUnityConnected({conn, settings, launcher, onStatus})` in `unity_launch_service.dart`: attach to a running instance first (never spawns a duplicate) → if not connected and not running, `launcher.launch()` (re-runs `init()` first when the path is empty, picking up the bundled-player auto-detect) → poll-connect 12×2 s while the engine boots → null on success / operator-facing error. Both controllers now take an optional `launcher` (passed from `_runLive` — read before the await to avoid the async-gap lint — and from ExerciseRecordScreen) and call the helper; their old dead-end error branches deleted. Installer rebuild kicked off so the shipped dashboard includes the fix.
**Why:** A cold-start installed system must be able to run a protocol without the operator knowing to open the Smart Game app first; dev-machine habits (Editor in Play mode) masked the gap.
**Verify:** `flutter analyze` on the 5 changed files → No issues found. Manual (installed build): open Protocol Builder → Run live with nothing running → status walks Connecting → Launching Unity → Waiting → preview appears. Also fixes Exercises → Record with ZED from cold start.

### 2026-07-07  15:25
**DO:** **Fix: launching Unity threw "Bad state: Process is detached".** User hit it in Live Exercise on the installed build. `UnityLaunchService.launch()` started the player with `ProcessStartMode.detachedWithStdio` then attached `_process!.exitCode.then(...)` — a detached `Process` only exposes `pid`, so touching `exitCode` throws synchronously; the catch marked the launch as error (even though the exe may have spawned) and the flow died. Never seen in dev because the Editor was always already running, so `launch()` had never actually executed a player exe until the bundled-install era. Fix: `ProcessStartMode.detached` (also removes the un-drained stdio pipes, which could have blocked the player mid-session once the ~64 KB pipe buffer filled), replace the exitCode watcher with `_watchLiveness()` (5 s `tasklist /FI "PID eq <pid>"` poll → flips status to notRunning when the player exits/crashes, keeping the home status line and `ensureUnityConnected`'s relaunch decision truthful), and `stop()` now uses pid-based `Process.killPid` in a try/catch. Installer rebuild kicked off (the 15:12 exe has the auto-launch fix but not this one).
**Why:** Second cold-start launch bug exposed by the installed system — dev-machine habits masked both. With this + ensureUnityConnected, all three flows (Live Exercise, Run live, Record with ZED) can start the bundled Smart Game player from nothing.
**Verify:** `flutter analyze` unity_launch_service.dart → No issues found. Manual after reinstall: Live Exercise → Start with nothing running → Unity player launches, status shows Running→Connected; close Unity manually → home status drops to "Not running" within ~5 s.

### 2026-07-07  15:35
**DO:** **`installer\build_unity.cmd` — scripted headless Unity player build** (wraps `Unity.exe -batchmode -quit -projectPath telehealth-unity -buildWindows64Player "Builds\Windows\Smart Game.exe" -logFile Builds\build.log`; guards: editor exe exists, Unity Editor not running, output exe present after build w/ antivirus hint). CRLF+ASCII from the start (lesson from build_installer.cmd). README table updated. Installer rebuilt 15:18 with BOTH cold-start launch fixes → `TeleRehabSetup-1.0.0.exe` 87.9 MB is the current shippable.
**Why:** User asked how the Unity exe is built; scripting it makes the two-step release (build_unity.cmd → build_installer.cmd) reproducible without Editor UI knowledge.
**Verify:** Ran it end-to-end → Unity rebuilt incrementally (log shows `BuildPlayer: start building target 19` + full size report, exit 0; exe timestamp unchanged because Unity 6's Bee pipeline skips rewriting outputs when no project inputs changed — expected, not a failure). Gotcha logged: an "is the exe fresh" check must look at the BUILD LOG, not the file timestamp.

### 2026-07-13  10:15
**DO:** **Windows Defender deleting the installer on the (IT-managed) target machine + no admin rights there to fix it.** Diagnosed rather than guessed: checked THIS machine first — `Get-MpThreatDetection` empty, current 87.9 MB installer intact, real-time protection on — so the quarantine is happening on the target, not here; and separately, `telerehab.iss` has `PrivilegesRequired=admin`, so even with Defender allowing it, a target with zero admin anywhere couldn't launch the installer at all (two stacked blockers, not one). User confirmed the target is **IT-managed** (someone else has admin/policy control there), which makes a **hash-based Defender allow rule requested from IT** the fastest fix — no exclusion or elevation needed from the user's own account. Made this repeatable: `build_installer.cmd`'s final step now runs `certutil -hashfile ... SHA256` on the produced exe and prints it automatically (verified the exact `for %%F in (...) do certutil ... | findstr /v "hash CertUtil"` construct under real cmd.exe via a throwaway test script — my Bash tool's POSIX shell doesn't count, PowerShell `cmd /c` with a temp .cmd file does). `installer/README.md` gained an **"If Windows Defender deletes/quarantines the installer"** section: ranked fixes that need NO admin action from the user (1. IT hash allow-list — recommended here; 2. Microsoft false-positive submission at wdsi/filesubmission, redo per rebuild; 3. code-signing cert for scale/durability), plus a flag that a true no-admin-anywhere target additionally needs the installer switched to `PrivilegesRequired=lowest` (not applied — asked-for-explicitly since it changes install location to per-user).
**Why:** Root cause is real and expected — an unsigned installer that silently launches a second exe (Unity) is a textbook dropper heuristic; the fix is reputation/allow-listing, not app code. Diagnosing which machine and which blocker (AV vs. privilege) before proposing fixes avoided prescribing the wrong one (e.g. code-signing money/time when a five-minute IT ticket solves it).
**Verify:** SHA-256 of the current `TeleRehabSetup-1.0.0.exe` confirmed two ways (`Get-FileHash` and `certutil`, matching): `9FBE06CF343FD59970615B4AC3767A7CE54F11B1B94389C30C6DA1E13EF6DED5`. Next real build will print its own hash automatically — no manual step needed going forward.

### 2026-07-13  10:40
**DO:** **Portable ZIP alternative — for "I don't want IT involved."** Clarified for the user that submitting to Microsoft's false-positive portal is NOT an IT-involving step (it's the developer submitting directly to Microsoft; the earlier answer had bundled it confusingly next to the IT-hash-allow option). Built `installer\build_portable.cmd`: stages `dashboard\` (Flutter release) + `unity\` (Unity player) + the VC++ redist as a **plain file the user runs manually** (not silently auto-executed from an installer's [Run] section) + a README, then zips via `Compress-Archive` → `installer\Output\TeleRehab-Portable-1.0.0.zip` (115.8 MB) — same certutil SHA-256 auto-print as the installer script. Rationale: the likely single biggest Defender trigger on `TeleRehabSetup-*.exe` is the combination "unsigned Inno Setup installer that silently launches a second executable" — a textbook dropper heuristic. Removing the installer wrapper entirely (just a folder to unzip and run) removes that specific pattern, with zero cost and no admin/IT action anywhere. Not a guarantee — the individual exes are still unsigned.
**Hit + fixed while building:** first `Compress-Archive` run failed mid-zip — `MsMpEng` (Defender's own real-time engine) had one of the freshly-robocopied DLLs open for scanning, a classic write-then-immediately-zip race. Not a real block; a bare retry a few seconds later succeeded outright. Verified integrity by extracting the zip fresh and confirming `dashboard\telehealth_dashboard.exe` + `unity\Smart Game.exe` are both present (242 files total). `.gitignore` +`installer/portable_stage/`.
**Why:** User explicitly doesn't want their org's IT involved at all. Gave the honest menu: no local switch makes Defender unconditionally accept an unsigned exe, but (a) a direct-to-Microsoft submission and (b) code-signing are both genuinely IT-free; (c) this portable zip is a free, immediate thing to just try first.
**Verify:** `installer\build_portable.cmd nobuild` → after one retry, zip built + extracted cleanly (242 files, both key exes present). SHA-256: `79C747A2923C2690B3EE39D74C43B97253DBFD0DE750B71B9735158DA6FD8696`. README gained a "Portable alternative" section documenting the build command and the Defender-scan race note.

### 2026-07-13  13:45
**DO:** **Root-caused the "Smart Game launches then dies" crash → ZED SDK version mismatch, upgraded project to SDK 5.4.** User's `Player.log`/`Player-prev.log` showed `Plugins: Failed to load '...sl_unitywrapper.dll' with error 'The specified procedure could not be found.'`, then the app dying either right after the CompleteExercise scene switch (prev run) or even earlier with `[Physics::Module] Initialized fallback backend` (id `0xdecafbad`, worse than before) — a progressively-worsening pattern consistent with a broken native plugin, not AV (ruled out separately: process DOES appear in Task Manager, so it launches; SmartScreen isn't blocking it). User had installed ZED SDK 5.4 on the target PC while the Unity project was still built against 5.2.3 ("procedure not found" = classic ABI mismatch between the Unity wrapper and an incompatible installed SDK). Chose to upgrade the project forward to 5.4 rather than pin the target back to 5.2.3. Found the plugin is a git-pinned UPM package (`telehealth-unity\Packages\manifest.json`: `com.stereolabs.zed` → `github.com/stereolabs/zed-unity.git?path=/ZEDCamera/Assets#v5.2.1`), confirmed available tags via `git ls-remote --tags` (v5.0.0…v5.4.0 exist), bumped the pin to `#v5.4.0`. Installed ZED SDK 5.4.0 on the dev machine (user's own action — I don't auto-download/run vendor installers); confirmed via `zed-config-version.cmake` → `5.4.0`; CUDA/driver unchanged (13.2 / 596.21).
**DO (rebuild):** Cleared `Builds\`, `installer\unity_payload\`, `installer\portable_stage\` first — **robocopy doesn't delete stale destination files**, so without clearing, the OLD `sl_unitywrapper.dll` (5.2.3) would have shipped alongside the NEW plugin files inside the same package. Ran `build_unity.cmd` (first attempt failed on its own "Editor is running" guard — user had Unity open; closed it, reran, exit 0, fresh exe timestamp confirmed). Discovered Stereolabs **renamed** the native plugin between releases: `sl_unitywrapper.dll` (5.2.x) → `sl_zed_c.dll` + `sl_zed_unity.dll` (5.4.x) — not a missing file, a naming change; verified clean build log (no `error CS`). Rebuilt both `build_portable.cmd "...\Builds\Windows"` (confirmed only the new DLLs present in the zipped payload, no leftover old ones) and `build_installer.cmd "...\Builds\Windows" nobuild`. The resulting `TeleRehabSetup-1.0.0.exe` came out surprisingly small (23.9 MB vs the earlier 87.9 MB) — didn't trust the log's "Successful compile" alone; verified for real by silently installing it to a scratch temp folder (`/VERYSILENT /DIR=...`) and confirming **305 MB actually laid down on disk** with both `dashboard\telehealth_dashboard.exe` and `unity\Smart Game.exe` present — genuine, just an unusually good LZMA2 solid-compression ratio this build, not missing data. Scratch install deleted after verification. README's "tested configuration" updated to 5.4.0 + a note on the plugin rename and the git-tag-bump procedure for future SDK upgrades.
**Why:** A silent ABI mismatch (loads, but a called function doesn't exist) is much nastier than a load failure — it can run fine for a while then crash deep into a session. Matching the git-pinned Unity plugin tag to the actually-installed SDK version is the one true fix; user chose to move the whole project forward to 5.4 rather than keep future capture PCs pinned to the older 5.2.3.
**Verify:** Unity batchmode build → exit 0, clean log, fresh `sl_zed_c.dll`/`sl_zed_unity.dll` present, no `sl_unitywrapper.dll` remnants anywhere in the new payload. Portable zip rebuilt (`installer\Output\TeleRehab-Portable-1.0.0.zip`, 116 MB) and installer rebuilt (`TeleRehabSetup-1.0.0.exe`, 23.9 MB, contents verified by an actual scratch-directory silent install). Next: user needs to re-test on the target PC (which already has SDK 5.4 installed) with this newly rebuilt package.

### 2026-07-17  10:15
**DO:** **`INSTALL.md` — operator-facing installation guide** (repo root). Ordered checklist for setting up a new capture PC: VC++ redistributable (auto/manual depending on installer vs portable zip), ZED SDK **5.4.0 exactly** (with the version-mismatch warning up front as the single most important rule — grounded in the real crash diagnosed 2026-07-13), ZED 2i camera placement, Unicorn Hybrid Black EEG (Optional — g.tec CSR dongle only, not built-in Bluetooth; Windows single-BT-stack caveat; COM port note), the app itself (installer vs portable zip, sibling-folder + Unblock notes), FSR insoles (Optional). Closes with first-launch pointer to the in-app onboarding guide and a troubleshooting section (Unity status check, Player.log location, Defender/SmartScreen pointer to `installer/README.md`). `installer/README.md` cross-links it at the top (dev-facing "build" doc vs this new operator-facing "install" doc).
**Why:** User asked for an installation guide listing what needs to be installed (CUDA version, Unicorn Hybrid Suite, etc.) — distinct from `installer/README.md`, which is about building the installer, not using it. All version numbers/facts pulled from what was actually confirmed this session (ZED SDK 5.4.0 + CUDA 13.2 re-verified via `zed-config-version.cmake` before writing), not guessed.
**Verify:** Re-read `Packages/manifest.json` + `zed-config-version.cmake` immediately before writing to confirm 5.4.0 in both places (matches the 07-13 upgrade). No download URLs included (per policy) — points to "Stereolabs' official site" / "g.tec's official site" instead of a guessed deep link.

### 2026-07-17  10:35
**DO:** **`VALIDATION.md` — sensor data validation protocol** (repo root). A run-this-checklist doc for confirming EEG + ZED 2i data is actually correct, not just streaming. Grounded in a real finding while writing it: checked `Assets/Scripts/SaveSystem` for confidence/quality persistence and confirmed **neither sensor's per-frame quality signal is saved to disk** — `zed_skeleton.json` never gets per-joint confidence, `eeg.csv` only has raw µV (no quality/artifact column); both only exist in the live stream (`signal_quality.dart`'s pre-flight gate). Documented as an explicit "Known gap" under each sensor, not glossed over. Tests: **E1** eyes-closed alpha test (physiological ground-truth, no reference hardware needed — flagged as the single most important outstanding test, never actually run/recorded despite the decode being verified correct back in June); **E2** absolute-scale calibration against Unicorn Recorder (ties to the still-uncalibrated `EegScaleUv`); **E3** pre-session sanity — documented the exact live thresholds from `SignalThresholds` (railed >5000µV, flat <0.5µV, need 6/8 clean); **Z1** known-distance test (deliberately did NOT invent a specific Stereolabs accuracy %, told the reader to check current vendor docs instead of trusting a guessed number); **Z2** goniometer joint-angle test against `Skeleton3D.activeAngle` (±5°, cited as clinical-goniometry literature convention, flagged for the user to confirm it's tight enough for their study); **Z3** repeatability/jitter (by-eye, no automated threshold exists); **Z4** pre-session sanity (zedMinJoints=8). Closes with a shared "known limitation" (no post-hoc audit today) and a blank Results Log table. `INSTALL.md` cross-links it under a new "After installing" section.
**Why:** Direct follow-up to explaining EEG/ZED validation methodology — user asked for it as an actual document/checklist rather than a one-off chat answer.
**Verify:** All numeric thresholds copied verbatim from `signal_quality.dart` (already read this session), not re-typed from memory. No fabricated accuracy specs — Z1 explicitly defers to vendor documentation instead of a guessed percentage.

### 2026-07-24  11:50
**DO:** **Diagnosed + fixed EEG connect failure: "The network location cannot be reached."** User reported Unicorn Suite showing connected while the dashboard still said EEG not streaming. Traced the full chain (`UnicornEegConnector.Connect` → `EegRecorder.Start` → `ExerciseController`'s `SessionContext.EegEnabled` block) and confirmed the failure is caught silently — logged only to Unity's own console/Player.log (`[EEG] Could not open COM5: ...` / `[Exercise] EEG connect failed on COM5; continuing without EEG.`), never surfaced to the dashboard beyond a generic "not streaming." User then supplied the actual `Player.log` error text: **"The network location cannot be reached."** — this is a distinct failure mode from "Access is denied" (port held by another process); it's the characteristic .NET/Win32 message for a Bluetooth SPP virtual COM port whose RFCOMM link isn't currently live, even though Windows still shows the device as *paired*. Ruled out the earlier "Unicorn Suite is hogging the port" theory on this evidence. **Fix:** added a bounded retry to `UnicornEegConnector.Connect()` (`Assets/Scripts/Sensors/Eeg/UnicornEegConnector.cs`) — up to 3 attempts, 500ms apart, blocking (acceptable since this only ever runs once, synchronously, during session start — not a hot path). Logs each retry attempt distinctly from the final failure.
**Why:** The one-shot connect had zero tolerance for a transient "Bluetooth link not re-established yet" condition, which is plausible and self-clearing within ~1 second in practice — exactly what a short retry fixes without any architecture change.
**Verify:** Confirmed Unity Editor closed, rebuilt via `build_unity.cmd` (fresh exe timestamp, build log has zero `error CS` — only pre-existing unrelated obsolete-API warnings in other files). Cleared `installer\unity_payload`/`portable_stage` first (stale-file precaution, same as the ZED 5.4 rebuild), then repackaged both: `TeleRehab-Portable-1.0.0.zip` (116 MB, matches prior size) and `TeleRehabSetup-1.0.0.exe` (88 MB, matches prior size — confirms the earlier 23.9 MB build was genuinely just an LZMA2 compression outlier, not a pattern). User still needs to verify the retry actually resolves the connection on the real target hardware — also flagged three non-code checks regardless of this fix: confirm Windows Bluetooth settings show the headset as *Connected* not just *Paired*, confirm the earlier Bluetooth power-saving fix hasn't reverted (e.g. via a Windows Update), and try opening COM5 with a plain tool (`mode COM5 baud=115200`) to isolate whether it's Unity-specific.

### 2026-07-24  12:20
**DO:** **Root-caused the real bug: the EEG COM port has never been configurable from the dashboard at all.** Retry fix above didn't resolve it. User reported three new facts: Windows Bluetooth only shows the headset as "Connected" while Unicorn Suite is actually running (not otherwise); the dashboard's EEG status never goes green during warm-up; and setting "COM5" in Settings didn't help. Checked `app_settings.dart`/`settings_screen.dart` and confirmed: the **only** COM-port picker in Settings is under FSR — there has never been an EEG-specific port field anywhere in the Flutter app. Unity's `ExerciseController.cs` reads `PlayerPrefs.GetString("EEGComPort", "COM5")`, a key **nothing in this app has ever written** — so the user's "COM5" selection was the FSR port picker (unrelated to EEG), and Unity was always stuck on a hardcoded default. Since classic-Bluetooth SPP COM ports only get their RFCOMM link established when a client actually opens them, Unity silently trying the wrong/stale port also explains why Windows never showed a connection during our attempts — we were never touching the headset's real bonded port. **Fix, mirroring the existing FSR wiring exactly:** (1) Unity — added `eegComPort` to the `TelerehabCommandMessage` DTO (`Network/TelerehabWebSocketServer.cs`) and a `PlayerPrefs.SetString("EEGComPort", msg.eegComPort)` write in `RemoteSessionController.Configure()` (`Network/RemoteSessionController.cs`), guarded the same `IsNullOrEmpty` way as the FSR fields; the configure-session log line now also prints the resolved port so future diagnosis doesn't need guessing. (2) Flutter — `AppSettings.eegComPort` (new pref key `eeg_com_port`) + `setEegComPort`; added to `toUnityConfig()`; **also** added directly to `ProtocolRunController.prepare()`'s hand-built configure_session payload (`protocol_run_controller.dart`) since Run live does NOT go through `toUnityConfig()` — that's the exact flow the user is testing, so missing it there would have shipped a fix that doesn't apply to their actual use case. (3) Settings screen — new EEG COM port picker under the EEG toggle (`settings_screen.dart`), reusing the same `availablePorts`/`_choice` widgets as FSR's picker, with a note that re-pairing can change the assigned port.
**Why:** A configuration path that silently doesn't exist is worse than a wrong value — the user reasonably believed they'd already fixed this by "selecting COM5 in Settings." Grounding this in the actual FSR wiring pattern (`configure_session` → PlayerPrefs → existing connector reads it as-is) meant no new plumbing concepts, just filling a real gap using code that was already proven working for FSR.
**Verify:** `flutter analyze` on the 3 changed Flutter files → No issues found. Unity rebuild completed clean (fresh exe, `build.log` has zero `error CS`). Both packages repackaged from that build and confirmed at their expected known-good sizes: `TeleRehab-Portable-1.0.0.zip` (116 MB) and `TeleRehabSetup-1.0.0.exe` (88 MB). User must find the headset's ACTUAL current COM port (Device Manager → Ports, or Windows Bluetooth → More Bluetooth options → COM Ports tab) and pick it in the new Settings UI — this fix doesn't guess the right port, it just makes it possible to set one at all. Still outstanding: confirm on real hardware that this actually resolves the "not streaming" symptom.

### 2026-07-24  13:00
**DO:** **EEG "connects, then disconnects during warmup" — fixed a completely silent failure path.** With the COM port now configurable, user reported EEG DOES show connected, then drops during "warmup." Traced the lifecycle: `_eeg` (`EegRecorder`) is only created and opened at the exact moment recording starts (`ExerciseController.cs` line ~431, inside the `start_recording` handler) — so "warmup" here is really the connector's first few seconds of life, not a separate pre-recording preview. `Live.connected = _conn.IsConnected && _conn.IsLocked` — port genuinely opens and the frame-counter lock is achieved (hence "connected" shows correctly at first), then the Bluetooth link drops mid-stream. Found the actual bug: `UnicornEegConnector.ReadLoop()`'s background-thread catch-all (`catch (Exception) { break; }`) had **zero logging** and never closed/nulled the port — a mid-session drop left absolutely no trace in `Player.log`, and `IsConnected`'s truth value depended on ambiguous OS/.NET behaviour rather than an explicit transition. **Fix:** the catch now logs `[EEG] Read loop ended unexpectedly: <reason>`, sets `_running = false`, and explicitly closes the port so `IsConnected` deterministically (and immediately) flips false with a diagnosable cause in the log.
**Why:** This doesn't fix the underlying Bluetooth instability (still suspected: same category as the original connect failure, likely power-management or CSR-dongle-vs-built-in-radio related) — but a silent failure is undiagnosable by definition. Told the user to re-check the two non-code things that fixed this exact headset's instability before: confirm the CSR dongle (not built-in Bluetooth) is in use, and confirm the earlier power-saving/USB-selective-suspend fix hasn't reverted.
**Verify:** Unity rebuild clean (fresh exe, zero `error CS`). Repackaged both: `TeleRehab-Portable-1.0.0.zip` (116 MB) and `TeleRehabSetup-1.0.0.exe` (88 MB), both at expected sizes. Next diagnostic step is user-side: reproduce with this build, then read the new `[EEG] Read loop ended unexpectedly: ...` line in `Player.log` — its exact message will state the real cause (Bluetooth transport gone vs. something else) instead of leaving it to guesswork.

### 2026-07-24  13:20
**DO:** **Actual root cause found from a full `Player.log`, not a snippet: `UnicornEegProbe` — a leftover dev diagnostic harness from the original June byte-decode validation — was auto-connecting to COM5 on every app launch and holding it for the app's entire lifetime, completely independent of and uncoordinated with the real session pipeline.** User's log showed two parallel, contradictory stories: `[EEG] Opened COM5` → `Locked. Counter offset = 41` → thousands of clean `RMS µV=[...] alpha=[...]` lines streaming continuously from the moment the app launched, entirely at the main menu, no session running — vs. the actually-configured session port, COM4 (set via the new Settings picker, believed correct), failing every time with `System.TimeoutException` on `SerialPort.Write` inside `StartAcquisition()`. This is the exact opposite of what both of us assumed: **COM5 was the correct, working port all along; COM4 (what the user found and set) was wrong.** Re-read `UnicornEegProbe.cs` class doc — self-described as a standalone one-shot validation harness ("Attach to any GameObject... press Play... Once this looks right we wire the connector into the capture pipeline") that was clearly never detached from its scene GameObject after that validation finished in June, so it shipped active in every build since. Its exclusive hold on COM5 also meant: even switching the Settings port to COM5 to match would have made the REAL recorder collide with the probe and fail with "Access is denied" — a further, different-looking failure the user would have hit next.
**Fix:** wrapped `UnicornEegProbe.Start()`'s connect logic in `#if UNITY_EDITOR / #else` — a `Debug.LogWarning` no-op in any built player, full original dev-harness behaviour preserved for future Editor Play-mode testing (matching what the class was actually designed for). `Update()`/`OnDestroy()` already null-guard `_eeg`, so no further changes needed.
**Why:** Three rounds of fixes (retry logic, configurable port, read-loop logging) were all correct and necessary but were fixing symptoms downstream of a completely different, self-inflicted resource conflict that only a full end-to-end log — not a single error line — made visible. This is why the "connects then disconnects" and "wrong port" theories both partially fit the evidence without ever fully explaining it.
**Verify:** Unity rebuild in progress. User must ALSO change Settings → EEG COM port from COM4 to COM5 once this build is installed — the fix stops the probe from squatting on the port, but doesn't change what port the real session uses; COM5 is proven correct by the user's own log, COM4 is not.
### 2026-07-27  12:36
**DO:** **Settings COM-port picker now shows only real, present ports and labels/auto-picks the paired EEG headset** — previously it listed every registered COM port as a bare "COMx" string with no way to tell which was the Unicorn. Root cause: `AppSettings.refreshPorts()` enumerated ports with `[System.IO.Ports.SerialPort]::GetPortNames()`, which returns name-only strings from `HKLM\HARDWARE\DEVICEMAP\SERIALCOMM`. On this rig that's three SPP ports for one headset — confirmed live via `Win32_PnPEntity`: **COM7** is the *outgoing* port bound to the headset MAC (`...VID&00010047_PID&F000\...60B647E1265E...`), while **COM6/COM9** are the CSR dongle's own *local incoming* ports (`...LOCALMFG...`, present whether or not anything is paired). Re-pairing bumps the outgoing COM number and can leave the old one registered, so the list grew and none of it was labelled. **Fix:** (1) new `SerialPortInfo` model (`lib/models/serial_port_info.dart`) — port, friendly name, `present`, `pnpId`, and an `isBoundBluetooth` getter (`BTHENUM` && !`LOCALMFG`) that flags the paired-device port. (2) `refreshPorts()` now runs a `Win32_PnPEntity` query (tab-safe `|~|`-delimited lines: COM · friendly name · present · pnpId), filters to `present`, sorts by COM number, and **falls back to the old `GetPortNames()`** if WMI is unavailable/empty (bare names, all marked present) so non-Windows/locked-down hosts still work. `availablePorts` kept as a `List<String>` getter over the new `ports` list so existing callers (FSR picker, `toUnityConfig`) are untouched. (3) `suggestedEegPort` = the single bound-BT port (null if 0 or >1 — never guesses when ambiguous); when set and the stored `eegComPort` is empty or no longer present, it's auto-selected + persisted. (4) Settings UI — both FSR and EEG pickers use a new `_portChoice` chip: shows the COM name, reveals the full device name on hover (Tooltip), and stars (★, accent border) the headset-bound port; EEG helper text rewritten to explain the star + present-only behaviour.
**Why:** User asked why reconnecting the EEG shows multiple ports instead of the one it's actually on. The old enumeration was correct-but-blind (it faithfully listed every port with no metadata to disambiguate). `Win32_PnPEntity` carries exactly the metadata needed — friendly name to label, `Present` to drop phantoms, and the PnP id to distinguish the paired headset from the dongle's own ports — so the picker can point straight at the right port instead of leaving the clinician to guess among three identical-looking "COMx" chips.
**Verify:** Grounded in this machine's real device tree — ran the exact enumeration query twice (once as Dart passes it, one verbatim argument) and confirmed clean `COM7|~|Standard Serial over Bluetooth link (COM7)|~|1|~|BTHENUM\...` output for all three ports, with COM7 the only `isBoundBluetooth` match. `dart analyze` on all three changed/added files → **No issues found**. Not yet exercised in a running build against a live re-pair cycle — the auto-select + phantom-drop behaviour should be confirmed on the real headset by unplugging/re-pairing and reopening Settings.

### 2026-08-05  10:55
**DO:** **Added a performance-testing kit** (user wanted to load-test the app; established LoadRunner is the wrong fit — single-user real-time desktop app, no multi-user server to load — and built the tests that actually measure it). Three artifacts: (1) **`tool/ws_perf_test.dart`** — a pure-Dart synthetic dashboard client for the Unity WebSocket bridge (`ws://localhost:8765`). Replicates the real wire protocol (`{type:command,action:…,…params}` for configure_session/start_session/start_recording/ping; parses `sensor_update`/`pong`). Measures broadcast **rate**, inter-arrival **jitter/stalls** (there's no sequence counter in `sensor_update`, so drops are inferred from timing), command **RTT** via ping/pong, and payload size; per-second CSV + summary. Modes: `--drive` (Unity streams at preview rate — no hardware/files needed, since `SensorUpdateCoroutine` broadcasts during preview), `--record` (soak incl. file I/O), `--clients N` (broadcast fan-out = the only meaningful "load" dimension). (2) **`tool/sample_resources.ps1`** — samples dashboard (`telehealth_dashboard`) + Unity (`Smart Game`/`Unity`) CPU% and working set to CSV for the UI/soak stages. (3) **`PERF_TESTING.md`** — runnable checklist doc (VALIDATION.md-style): Stage 1 pipeline throughput/latency, Stage 2 Flutter DevTools UI-smoothness pass with dashboard-side mocks, Stage 3 endurance/soak, plus Unity Profiler companion steps and suggested acceptance targets.
**Why:** The app's real perf risks are real-time latency/throughput/jitter/endurance on one machine, not concurrency. Grounded every metric in existing instrumentation (`clockRttMs` ping/pong, EEG `FramesDropped`) and the actual broadcast cadence (`_updateInterval`).
**Verify:** `dart analyze tool/ws_perf_test.dart` → No issues found; `--help` runs. Sampler smoke-tested (2 s, `explorer`) → valid CSV. **Discovered the machine enforces `AllSigned` via MachinePolicy (GPO)** — unsigned `.ps1` won't run via `-File` even with `-ExecutionPolicy Bypass`; documented the working scriptblock invocation (`[scriptblock]::Create((Get-Content -Raw …))`) and verified it binds params + writes CSV. Could NOT do a live end-to-end harness run: Unity isn't running and port 8765 isn't listening — the user must start Unity (or the dashboard) first; the Dart harness itself is confirmed to start, connect-fail cleanly, and print usage.

### 2026-08-05  11:25
**DO:** **Ran Stage 1 (pipeline throughput/latency) live against the running Unity build and recorded results.** Bridge confirmed listening on 8765 (Smart Game PID 37868). **Findings:** (1) Unity broadcasts `sensor_update` at **~5 Hz** (mean 4.88, `_updateInterval`≈200ms) — this is the dashboard live-view cadence ceiling; recorded data (ZED/EEG) is written at true sensor rate during recording, so 5 Hz affects live smoothness, not data fidelity. (2) **Command RTT** median 4 ms / p95 14 ms / max 18 ms on localhost — excellent. (3) **Jitter** p95 ≈ 206 ms, tight around the 200 ms nominal, no stalls. (4) **Fan-out to 5 concurrent clients has zero measurable impact** (rate 4.89 Hz, RTT median 5 / p95 10 ms) — measured passively against the live stream. (5) **Warm-up ≈ 21 s** is the ZED init timeout (`[Exercise] Sensors not ready after 20s; previewing anyway` — no camera attached), not scene-load cost. **Robustness finding:** repeated `--drive` (configure+start_session) re-drives reload the capture scene and re-init the ZED without releasing the prior instance; after ~3 re-drives with no camera, sensor init **hung** (the 20 s "previewing anyway" fallback stopped firing) — reachable in real use if a clinician starts a new dashboard session without returning Unity to the menu. Flagged to verify with a real camera. **Harness fix:** added settle-and-rearm after start_session (discards the previous session's still-draining frames so measurement latches onto the reloaded stream, fixing a spurious 0 Hz window), and a clearer timeout message. Documented "drive from menu / measure a running session passively" in `PERF_TESTING.md`.
**Why:** Live numbers give the user a real baseline and turned up two things worth knowing — the 5 Hz live-view ceiling and the re-drive sensor-init hang — that only an end-to-end run against the actual engine surfaces.
**Verify:** `dart analyze tool/ws_perf_test.dart` → No issues found after both edits. Results are from real runs (driven 60 s single-client; passive 45 s 5-client; 10 s passive probe confirming recovery). Re-drive hang confirmed from `Player.log` (session at 11:23:34 logged "Closed without recording" but never reached "previewing anyway"). **Unity is currently left in a stuck non-streaming reload state** — needs a return-to-menu (Close) or a build restart before further runs. Stages 2 (Flutter DevTools UI) and 3 (soak) not yet run.

### 2026-08-05  12:05
**DO:** **Ran Stage 2 (dashboard UI smoothness) and started Stage 3 (soak).** Stage 2: launched the dashboard in **profile mode** (`flutter run -d windows --profile`; profile, not debug — debug frame times are unrepresentative) with dashboard-side mocks animating the live monitor. **Quantitative (inline CPU/RAM sampler, 60 s):** CPU steady ~10.6% of 16 cores (~1.7 cores), working set flat at ~314 MB (+1 MB drift = no leak), ~79 fps. **Visual (user's DevTools Performance):** frames mostly fine BUT recurring red **shader-compilation** jank — Skia compiling gradient/glow shaders at first use (sources: EEG radial-gradient blobs `eeg_panel.dart`, plantar/foot heatmaps, neon avatar glow; each distinct shader compiles once → switching avatar styles/opening panels keeps hitting new ones). **Tried the modern fix — Impeller** (`--enable-impeller`): log confirmed `Using the Impeller rendering backend (OpenGL)` and it launched, but **rendered a black screen** on this Windows GPU/driver — the desktop OpenGL Impeller backend is still immature in Flutter 3.44, so it's not viable here. **Decision (user): accept the shader jank as-is** — it's transient/first-use and cosmetic; the app is otherwise smooth (stable CPU, flat memory, 79 fps), which is defensible for a research tool. Documented SkSL warm-up (`--cache-sksl` → `--bundle-sksl-path`) as the available Skia-path fix if polish is wanted later. Stage 3: launched a **20-min driven+record soak** (`ws_perf_test.dart --drive --record --seconds 1200 --patient SOAK-TEST`) from Unity's main menu (clean single drive — avoids the re-drive hang), plus a parallel Unity memory sampler (every 5 s). Confirmed the soak connected, cleared the 21 s ZED warm-up, and is recording. Results pending (~20 min).
**Why:** Stage 2 needed both halves — the sampler proves no leak / bounded CPU, DevTools proves whether frames are smooth. The shader finding is real but correctly classified as cosmetic once Impeller (the clean fix) proved unusable on this hardware.
**Verify:** Stage 2 numbers from a real 60 s profile-mode run; Impeller black-screen reproduced live (log shows the backend engaged, so the flag worked — the renderer itself failed). Stage 3 soak + sampler running in the background; will chart Unity working-set trend + broadcast rate_hz on completion. Leftover `Sessions/SOAK-TEST/…` folder to delete after. **Note:** Impeller-on-Windows should be re-tried on a future Flutter version — it would eliminate the shader jank outright if the desktop backend matures / a D3D backend lands.

### 2026-08-05  12:30
**DO:** **Stage 3 (endurance/soak) complete — clean pass.** 20-min driven+record run (`ws_perf_test.dart --drive --record --seconds 1200`) + parallel Unity memory sampler (5 s cadence). **Pipeline:** broadcast rate held flat at ~4.92 Hz across all four 5-min quarters (Q1 4.91 / Q2 4.92 / Q3 4.92 / Q4 4.92), zero seconds below 4 Hz, worst single stall 238 ms; RTT median 3 / p95 7 / max 22 ms (even tighter than the short runs). **Unity process:** CPU steady ~10.5% (~1.7 of 16 cores) start-to-finish, no drift; private memory rose ~100 MB (1007→~1110 MB) during the first ~10 min then **plateaued** for the rest — bounded warm-up/cache growth, not a linear leak. **Overall performance testing verdict (all 3 stages): PASS.** Stage 1 bridge healthy (5 Hz, single-digit-ms RTT, fan-out to 5 clients free); Stage 2 UI smooth except cosmetic shader-compilation jank (Impeller fix black-screens on this Windows GPU → deferred); Stage 3 no endurance/leak issues. The only real perf item is the deferred shader jank (P2 backlog).
**Why:** A soak is the only stage that catches slow leaks/drift; confirming the rate stays flat and memory plateaus over 20 min is the evidence that matters for multi-minute research sessions.
**Verify:** Numbers computed from the two CSVs (perf_soak.csv 1200 rows; soak_resources.csv ~234 rows) — quarter-by-quarter rate means and the memory plateau both checked with awk, not eyeballed. Caveat: 20 min is a moderate soak and ran with no real sensors (ZED no body, eeg off, fsr mock), so a real-hardware session (EEG 250 Hz writes, ZED body frames) would exercise more file I/O — worth one real-sensor soak before a study. **Cleanup pending: delete the throwaway `Sessions/SOAK-TEST/…` folder.**

### 2026-08-05  13:05
**DO:** **Added `REFERENCES.md`** — literature/standards backing the performance-testing methodology, mapped stage-by-stage, in APA + BibTeX. Sources pulled from live web searches (not memory) and each tagged by provenance (`[STD]` standard, `[PR]` peer-reviewed, `[BOOK]`, `[GREY]` vendor, `[PP]` preprint). Core citations: ISO/IEC 25010 (performance efficiency), Meier et al. 2007 (endurance/soak taxonomy), Jain 1991 + Molyneaux 2014 (measurement), Miller 1968 / Nielsen 1993 + MacKenzie & Ware 1993 (latency & responsiveness — the last fully verified: INTERACT/CHI '93 pp. 488–493, DOI 10.1145/169059.169431), RFC 3393 (jitter = delay variation), RFC 5905/NTP (the ping/pong clock-offset the code uses), Huang et al. 1995 + a software-aging survey (soak/leak rationale), and Ye et al. 2024 *Frontiers in Neuroscience* 18:1416719 (peer-reviewed teleoperation-latency — replaced the earlier arXiv preprint).
**Why:** User asked whether the testing approach is backed by research and wants citeable, linked sources for a writeup. Grounding each stage in established methodology shows the approach isn't ad-hoc.
**Verify:** Every link came from actual WebSearch/WebFetch results this session, not fabricated. Entries I could not fully verify are explicitly marked `⚠ [verify]` (some DOIs/pages; the aging-survey authorship; two supporting HCI papers) rather than invented. Flagged in the doc that the shader-jank/Impeller material has NO peer-reviewed basis and must be cited as vendor documentation. Numeric latency thresholds are from search summaries — doc tells the reader to confirm against the source before quoting.

### 2026-08-05  13:25
**DO:** **Wrote `PERFORMANCE_TEST_REPORT.md`** — a professional, thesis-style report of the whole performance-testing effort: executive summary + verdict table, introduction/scope (incl. the LoadRunner rationale), test environment, methodology with metric definitions, per-stage procedure/results/analysis for all three stages (with the real measured numbers), a findings-summary matrix (F1–F6 with severity + disposition), limitations/threats-to-validity, recommendations, an APA reference list, and appendices (reproduction commands + related docs). Citations woven inline — ISO/IEC 25010, Meier et al. 2007, Jain 1991, Molyneaux 2014, Miller 1968, Nielsen 1993, MacKenzie & Ware 1993, RFC 3393, RFC 5905/NTP, Huang et al. 1995, Ye et al. 2024 — all cross-referenced to `REFERENCES.md`.
**Why:** User asked for a detailed professional document of the steps + findings, with citations, suitable for a research/coursework writeup.
**Verify:** All figures taken from this session's actual runs (Stage 1 60 s single + 5-client fan-out; Stage 2 60 s profile-mode sampler + DevTools; Stage 3 20-min soak + 5 s memory sampler), not re-typed from memory. Cosmetic shader jank + Impeller-black-screen recorded honestly as F4; the re-drive hang as F6; the synthetic-sensor + 20-min-duration caveats stated in Limitations. Report points the reader to REFERENCES.md for provenance tags and to confirm secondary numeric thresholds before quoting.

### 2026-08-05  14:00
**DO:** **Published a step-by-step testing METHODOLOGY document as a rendered Artifact** (academic register, per user's chosen format/tone). URL: https://claude.ai/code/artifact/f84686eb-1245-46f5-b659-4717ec06d2e1 . Grounded the visual identity in the project's "Daydream" system (violet #7C4DD6 accent, cool violet-tinted neutrals — not a template) with a 3-role type system (Charter/Cambria serif for prose, uppercase system-sans eyebrows, mono tabular-nums for data). Contents: §1 system-under-test + environment (Fig 1 architecture, mermaid), §2 instruments + test protocol (Fig 2 sequence diagram), §3 metric derivation (Figs 3a/3b — RTT ping/pong + jitter inter-arrival), §4–6 the three stages each with numbered procedure steps + a mermaid flowchart + result tables, and Stage 3's two **real-data SVG charts** (Fig 9 rate-over-soak, Fig 10 memory-over-soak) built from the actual 1200-pt/232-pt soak CSVs with hover tooltips. Two **screenshot-spec placeholders** (Fig 5 harness terminal, Fig 7 DevTools timeline) describe exactly what the user must capture. Both light/dark themes supported via CSS tokens; diagram/chart "plates" fixed light by design.
**Why:** User asked for a detailed, visual, step-by-step document of the testing carried out, academic register, visuals where possible / described where not — answered via AskUserQuestion (Artifact + academic + all 4 visual types).
**Verify:** Charts use the real extracted series (rate mean 4.92, dips to 4; priv-mem 1007→~1110 then plateau) — not fabricated. Fixed a `.tip` CSS typo (transl→translate) and simplified an annotation coord before publishing; corrected a subscript-notation inconsistency in the jitter diagram and republished to the same URL. NOT visually verified (can't render the page here) — mermaid + inline-SVG charts should render in the Artifact runtime, but the user should open it and flag any diagram parse errors or layout issues for adjustment. Figs 5 & 7 are intentional capture placeholders, not real screenshots.

### 2026-08-10  15:20
**DO:** **Real-hardware EEG+ZED validation run + report update + Word doc.** Ran a live 8m33s recorded session (Unicorn EEG on COM7 + ZED 2i tracking a body), driven from the dashboard, measured passively + resource-sampled. **Results:** EEG **0 dropped samples** across 128,373 samples at exactly 250.1 Hz (counter sequential 1→128373, locked on COM7, no disconnect) — lossless; pipeline held under load (rate 5.33 Hz, RTT median 5 ms, payload 2815 B ≈3.3× synthetic); Unity CPU 16.5% / mem 2.79→2.98 GB rising. **Two refinements the synthetic runs missed:** (1) recorded **ZED skeleton is ~5.33 Hz** (persisted on the ~200ms update tick, NOT camera rate; full rate recoverable offline from the 750 MB SVO) — this corrects the report's earlier "recorded at true sensor rate" claim; (2) recording memory grows with session length (EEG/skeleton buffered in RAM until save). Updated **PERFORMANCE_TEST_REPORT.md → v1.1**: new §7 Real-hardware validation, corrected §4.3, renumbered §7→8/8→9/9→10, fixed the (§8)→(§9) cross-ref, added F7 (skeleton rate), F8 (recording memory), F9 (EEG lossless — pass), updated exec summary/verdict/limitations/recommendations. **Generated doc files:** wrote a MD→HTML converter (`scratchpad/md2doc.ps1`, run via scriptblock under AllSigned) → `PERFORMANCE_TEST_REPORT.html` (27.6 KB), then Word COM `SaveAs2` → `PERFORMANCE_TEST_REPORT.docx` (36.2 KB).
**Why:** User asked to fill the report's real-hardware gap, produce a document file, and identify required screenshots.
**Verify:** EEG-drop count computed by awk over the saved `eeg.csv` counter column (0 gaps), not asserted; skeleton rate read from `session.json` (`sampleRateHz: 5.33`, `sampleCount: 2737`) and cross-checked against duration. `.docx` produced via Word (first `SaveAs` failed on a `[ref]` psobject coercion; `SaveAs2` with plain args succeeded) — it's an HTML→Word conversion, so formatting should be eyeballed and tidied in Word; the `.md` remains canonical (re-run the converter after edits). The real `P-002` session (~775 MB incl. SVO) left in place — user's data, not deleted. Screenshots still outstanding (user-screen content I can't capture).

### 2026-08-10  15:45
**DO:** **Embedded the user's screenshots into the report + regenerated all formats.** User provided 4 screenshots (2 DevTools timelines, 2–3 live-monitor shots); found them in `Pictures\Screenshots`, verified two by reading them, copied the best of each into repo `docs/figures/` (`devtools-frames.png` 66 KB, `realhw-monitor.png` 505 KB). Added the new data they reveal to §7: **EEG Signal Quality Good / Artifact Low** (good electrode contact — complements the 0-drop result) and **live-monitor render cost under real load** (raster avg ~14.6 ms, ≈68 fps). Placed **Figure 1** (DevTools frames, §5) and **Figure 2** (live monitor, §7) via `![](docs/figures/…)` markdown. Extended the MD→HTML converter to render figures and, crucially, to **embed images into the DOCX** — Word's HTML import wouldn't embed relative/`file:///` `<img>` (docx stayed 39 KB), so switched to a two-pass approach: pass 1 HTML→docx (captions only, `<img>` stripped), pass 2 reopen + `Selection.Find` each caption + `InlineShapes.AddPicture(LinkToFile=false, SaveWithDocument=true)` aspect-locked to page width. DOCX now **377 KB with both figures embedded**; HTML keeps relative `<img>` for browser viewing; MD keeps portable image refs.
**Why:** User asked to take the required screenshots and embed them into the doc file.
**Verify:** Console confirmed `embedded: devtools-frames.png` / `realhw-monitor.png`; docx grew 39→377 KB (proves the ~570 KB of images are in the file, not linked). Screenshots contain no PII (pseudonymous P-002, cartoon avatar) — consistent with the participant-privacy design, safe to commit. `.docx` is an HTML→Word conversion + COM image insert — recommend a visual pass in Word to confirm figure placement/sizing. Methodology **artifact** NOT yet updated with these images (would need base64 data-URIs + republish) — offered as a follow-up.

### 2026-08-12  09:30
**DO:** **Added the mocked-data soak visuals to the formal report** (user noted the report had the real-hardware screenshots but not the synthetic-soak charts that the methodology artifact carried). Rendered two static PNG line charts from the actual soak CSVs via **.NET System.Drawing** (`scratchpad/make_soak_charts.ps1`, no dependencies): `soak-rate.png` (broadcast rate over 20 min, flat at 5 Hz with dips to 4) and `soak-memory.png` (engine private memory, rise-then-plateau) → `docs/figures/`. Added them to §6 as **Figure 2** (rate) and **Figure 3** (memory); renumbered the §7 real-hardware live-monitor to **Figure 4**; §5 DevTools stays Figure 1 — figures are now sequential by position. Registered both charts in the md2doc `$figs` array and regenerated: **DOCX 437 KB with all four figures embedded**, HTML with relative imgs, MD with portable refs.
**Why:** The report's Stage 3 (mocked-data soak) was tables-only; the user wanted its visuals (the rate/memory-over-time charts that exist in the methodology artifact) in the report document.
**Verify:** Charts drawn from the real 1,200-pt / 232-pt soak CSVs (not redrawn from memory); read both PNGs back to confirm legibility (memory chart clearly shows the plateau; fixed a label overlap on the rate chart before finalising). Converter console confirmed all four `embedded:` lines; docx 39→437 KB. `.md` remains canonical; re-run `make_soak_charts.ps1` + `md2doc.ps1` after data/report changes.

---

### 2026-08-19  14:20
**DO:** **Converted the plantar-pressure module from two insoles to one** (hardware changed: one insole, four FSR pads on a fixed foot = RIGHT, Arduino CSV `millis,toe,medial,lateral,heel` @100 Hz + inline `#MARK`/`#BASELINE`). Single source of truth added on both sides (`PressureConfig`, Unity + Dart: insoleCount=1, foot=right, channelNames=[toe,medial,lateral,heel], adcMax=1023, baudRate). Built a threaded single-insole capture path mirroring the EEG pipeline — `FootPressureSource` (background serial reader keeping EVERY line + a mock 100 Hz source), `PressureRecorder` (drain-all into the session list, decoupled from the 5 Hz broadcast tick), `FootPressureCsvWriter` (fsr.csv **schema v2**: `timestamp_utc,arduino_ms,t_ms,toe,medial,lateral,heel`, plus `pressure_markers.csv` `arduino_ms,kind,label`). `#MARK`/`#BASELINE` are parsed and persisted with their Arduino clock intact. Manifest bumped to **schemaVersion 2** recording `insoleCount`/`foot`/`channelNames`/`markerFile`. Broadcaster emits single-foot `plantar{foot,insoleCount,zones{toe,medial,lateral,heel},…}`; the two-insole path is GUARDED behind `InsoleCount==2`, not deleted. **ADC-scale bug fixed** (promoted to scope): `Norm` and the avatar FeetColorer divided raw by 255 (8-bit) while the ADC is 10-bit → every loaded pad flat-topped; both now use `PressureConfig.AdcMax` (1023). Flutter: `FootZones` renamed midInner/midOuter→**medial/lateral** everywhere (wire/CSV/UI/logs — midInner/midOuter retired), `PlantarData` single-foot, reader **branches on the fsr.csv header** so v1 two-insole recordings still open, live panel + replay + participant view render one foot with a single total-load line.
**Why:** New hardware is a single four-pad insole. The old code baked "two feet" into a stored file format, downsampled the recorded stream to the 5 Hz broadcast tick (and the old serial reader discarded backlog to the latest line — ~95% sample loss), and normalised on the wrong ADC scale — all on the exact path we collect research data on.
**Verify:** Unity C# — compiled the full Assembly-CSharp (105 files) with Unity's own Roslyn against Unity's prebuilt package DLLs (`scratchpad/compile_check.ps1`): **0 errors**. Flutter — `flutter analyze` clean (no new issues). 100 Hz path — `tool/verify_pressure.dart --sim --seconds 60` (isolate producer @100 Hz + 5 Hz drain, exercising the drain-all design): **6001 rows, 100.0 Hz, median Δarduino_ms 10 ms, p95 10 ms**; the OLD drain-to-latest kept only 301 rows (5%). Real-hardware acceptance check pending on the target machine: record 60 s (real Arduino or FSRConntype=Mock), then `dart run tool/verify_pressure.dart --file <session>/fsr.csv` (expect ~6000 rows, median ~10 ms). NOTE: `PressureConfig.BaudRate` default 115200 must be confirmed against the firmware's `Serial.begin`.

---

### 2026-08-19  16:05
**DO:** **Serial handshake + preview streaming for the single-insole pressure path** (follow-up to the two→one conversion). The firmware waits for commands (prints `#READY`, then `Z`=baseline / `S`=stream / `M`=marker / `X`=stop); the reader was receive-only, so it would have got `#READY` then silence. Wired the outbound handshake in `SerialFootPressureSource`: on `#READY` send `Z`, on `#BASELINE` send `S` (Interlocked state machine + a watchdog thread that sends Z/S anyway if a line is missed, so a reset hiccup never fails silently), `M` via `SendMarker()`, `X` on `Dispose()`. `Z` always precedes `S`. Marker path: Unity's existing `MarkEvent` command is now handled in `ExerciseController` → `_pressure.SendMarker()`, and the Flutter protocol runner fires `conn.markEvent()` on EVERY marker (`_appendMarker`), not just the manual button — so each protocol block boundary emits an Arduino `#MARK` into `pressure_markers.csv` on the Arduino clock. Parser robustness (item 3): unrecognised `#` lines (`#CMDS`/`#COLUMNS`/`#SAMPLE_HZ`/unknown) are logged at info and ignored, never throw/drop; `#WARN` is surfaced as a warning; data lines still require 5 fields with field 0 = Arduino ms. Live-during-preview restored (item 4): the pressure SOURCE now opens at **preview** (`StartPreview`→`StartPressureSource`) so the pre-record pad check has live data; disk capture stays gated on Record via new `PressureRecorder.BeginCapture()` (drains preview backlog, then persists) so fsr.csv is still exactly the record window. Baud (item 2): logged at connect with explicit "config default 115200" vs "PlayerPrefs override" so a wrong value shows in the log instead of presenting as silence.
**Why:** Item 1 was a blocker — no outbound command = no data, indistinguishable from a baud mismatch. `M` is the real payload: persisting `#MARK` is pointless if nothing emits them, and these are the only thing aligning pressure to EEG/skeleton. Pad check before recording needs live preview or a dead channel is a lost participant.
**Verify:** Unity `Assembly-CSharp` recompiled with Unity's Roslyn — **0 errors**; `flutter analyze` clean. Handshake/marker round-trip needs the real Arduino (or FSRConntype=Mock, which now emits `#READY`/`#BASELINE` and answers `M`) — pending on the target machine. NOTE: `Z` (baseline) fires automatically when the source opens at preview — insole must be unloaded then; flag if an explicit baseline trigger is wanted. `S` is sent at preview (streaming) not record, because the pad check requires live data — recording/disk stays gated on Record.

---

### 2026-08-19  17:40
**DO:** **Baseline persistence + loaded-baseline safeguards for the single insole.** GAP fix (priority): the `#BASELINE` values were consumed to drive the handshake but never stored — raw FSR is unanalysable without the offset. Now parsed into `PressureMarker.values` ([toe,medial,lateral,heel] + Arduino ms) and (a) written into `session.json` under `fsr` as `baseline`/`baselineArduinoMs` (last baseline before Record wins), and (b) every `#BASELINE` is written into `pressure_markers.csv` with its Arduino timestamp (so a mid-session/preview re-baseline is recoverable). `PressureRecorder` now accumulates markers ALWAYS (preview + record, so preview baselines survive) while samples stay gated on Record; it tracks the latest baseline across preview. Sanity check: `PressureConfig.BaselineMaxAdc` (default 80/1023) → `PressureRecorder.BaselineUnderLoad` → broadcast as `plantar.baselineUnderLoad` → **visible red banner** in the plantar panel ("Baseline captured under load. Remove foot and re-baseline."), not just a log. Manual re-baseline: new `rebaseline` command (`TelerehabCommand.Rebaseline`) + a preview-only top-bar button (`svc.requestBaseline()`) → Arduino `Z`; the source now (re)sends `S` on every `#BASELINE` so streaming resumes after a mid-session re-capture. Mock models it end-to-end (emits `#READY`/`#BASELINE,0,14,11,10,15`, answers `Z`/`M`). Verifier extended: `dart run tool/verify_pressure.dart --session <folder>` checks fsr.csv rows/timing + pressure_markers.csv (MARK vs BASELINE counts) + session.json baseline in one command.
**Why:** Raw values offset from an unknown baseline can't be zeroed offline and the baseline can't be reconstructed post-session — it blocked analysis. A baseline captured with the foot already in the shoe is silently wrong (range compressed downward), so it needs a visible warning and a one-click re-capture, not just a log line.
**Verify:** Unity `Assembly-CSharp` recompiled with Unity's Roslyn — **0 errors**; `flutter analyze lib tool` clean. Full baseline round-trip (session.json + markers file + warning + re-baseline) needs the Arduino or FSRConntype=Mock on the target machine — the mock exercises the whole path headless. `--session` verifier ready for the acceptance run.

---

### 2026-08-20  10:15
**DO:** **Firmware-format parser fixes + faithful mock + headless mock acceptance run.** (1) `#BASELINE_BEGIN` is now an explicit control-line case: logged, ignored, does NOT advance the handshake (only the numeric `#BASELINE,<ms>,<toe>,<medial>,<lateral>,<heel>` triggers 'S'). (2) Removed the 4-values-only fallback in `ParseMarker`; a `#BASELINE` without millis+4 values now logs a malformed warning instead of being silently accepted. (3) Refactored the handshake into `FootPressureSourceBase` so it is SHARED — the mock is now a command loopback (`SendCommand` interprets Z/S/M/X), so it drives the exact `#READY`→'Z'→`#BASELINE_BEGIN`→`#BASELINE`→'S' path real hardware does (previously it bypassed the handshake and never emitted `#BASELINE_BEGIN`). Mock emits `#BASELINE_BEGIN` then the numeric baseline after a ~500 ms unloaded pause, streams only after 'S', and forces a loaded baseline via PlayerPrefs `PressureMockLoadedBaseline`. (4) Fixed the mock's timestamp model: it now stamps a synthetic DEVICE clock advancing exactly 10 ms per sample (like an Arduino millis at 100 Hz), paced to real time via a bounded catch-up burst — so recorded `arduino_ms` deltas are a true 10 ms rather than the host `Thread.Sleep` ~15 ms granularity leaking into the device clock.
**Why:** The firmware was changed to millis-first `#BASELINE` (matches the data line) and added a separate `#BASELINE_BEGIN` pre-amble; the parser had to match exactly and stop silently accepting malformed lines. The mock's whole purpose is to catch divergence from hardware, so it must exercise the real handshake and a faithful device clock.
**Verify (headless, real capture classes via scratchpad/harness — Unity Roslyn 0 errors, flutter analyze clean):** handshake log shows `#READY`→'Z', `#BASELINE_BEGIN` ignored, numeric `#BASELINE`→'S'. Re-baseline works; forced-loaded → `BaselineUnderLoad=True`; cleared → `False`. 60 s record with 3 marks → `verify_pressure.dart --session`: **fsr.csv 6006 rows, 100.0 Hz, median/p95/max arduino_ms delta = 10 ms**; **pressure_markers.csv 3 MARK + 4 BASELINE**; **session.json baseline=[13,17,9,19]@5530ms, schemaVersion 2, foot=right, insoleCount=1**. In-app UI checks (live-preview render, banner rendering, button, replay one-foot vs v1 two-foot) still require the running app — logic verified, on-screen rendering pending the target-machine run.

---

### 2026-08-20  11:30
**DO:** **Baseline-timing hardening before hardware run** (real firmware takes ~2 s to return #BASELINE: 500 ms settle + 200 samples @ 5 ms). (1) Raised the handshake watchdog baseline window `BaselineTimeoutMs` 3000→**6000 ms** (≥5 s) and `ReadyTimeoutMs` 2500→3000, so the fallback 'S' can't fire mid-capture on real hardware. (2) Mock baseline delay 500 ms→**2000 ms** (`MockBaselineCaptureMs`) so it stops being optimistic about the capture time. (3) **Duplicate-'Z' guard**: every 'Z' (handshake, watchdog, manual re-baseline) now routes through `SendBaselineCommand`, which suppresses a second 'Z' while a capture is in flight (flag set on 'Z', cleared on #BASELINE, with the 6 s window as a lost-baseline fallback). Prevents a spurious/duplicate 'Z' (watchdog race or double button-press) from queueing a second capture behind the firmware's blocking window — which could re-baseline with the foot now loaded.
**Why:** A watchdog shorter than the real ~2 s capture would force 'S' mid-capture; the firmware blocks during capture and buffers late commands, so a stray 'Z' would trigger an unwanted (possibly loaded) re-baseline.
**Verify (headless harness, real classes; Unity Roslyn 0 errors):** numeric `#BASELINE` now arrives at 2000 ms device-clock (was 500). Double-press test logs `'Z' suppressed — a baseline capture is already in progress` and yields exactly ONE new baseline. Watchdog (6 s) never fires (baseline at ~2 s). Re-verified: **fsr.csv 6007 rows, 100.0 Hz, median/p95/max delta = 10 ms; pressure_markers.csv 3 MARK + 5 BASELINE; session.json baseline=[13,16,13,17]@14670 ms (last before Record), schema v2 / right / 1**.

---

### 2026-08-21  11:20
**DO:** **Two FSR indicators so the port/streaming state is obvious.** (1) Port auto-suggest: `SerialPortInfo.isUsbSerial` flags USB-serial/Arduino ports (VID_2341, or names CH340/CP210x/"USB Serial Device") — the symmetric opposite of the EEG's `isBoundBluetooth`; `AppSettings.suggestedFsrPort` picks the single USB port; `refreshPorts` auto-selects it when the saved FSR port is gone/empty (mirrors the EEG auto-select); the Settings FSR picker now stars it with tooltip "USB serial (Arduino) — likely the FSR insole" and a help line telling the user not to reuse the EEG's Bluetooth COM. `_portChoice` gained a `suggestHint` so FSR and EEG show their own suggestion text. (2) Live FSR status is now 3-state: broadcaster emits `sensors.pressureStreaming` (set by ExerciseController from whether `PressureRecorder.SamplesReceived` increased since the last broadcast); the top-bar chip shows red=Offline (port not open), amber=No data (port open but no pad samples — handshake/baud problem), green=Connected (data flowing). SensorStatus parses the new field (defaults false).
**Why:** The user spent a session pressing pads with nothing showing because the FSR was pointed at a Bluetooth COM (COM9) instead of the Arduino (COM3) AND running a stale build that never streams — both states were invisible. The ★ points at the right port; the amber dot distinguishes "port open but silent" from "streaming", which is exactly the missed signal.
**Verify:** Unity Roslyn 0 errors; `flutter analyze` clean. Nice property: the amber "No data" state shows even against the OLD Unity build (it never sends `pressureStreaming` → defaults false), so it immediately surfaces the silent-stream problem without needing the rebuild.

---

### 2026-08-21  12:10
**DO:** **Stop the ~20 s "warming up sensors" stall on FSR-only sessions.** `WarmupAndPreviewCoroutine` (and the early-Record `BeginRecordingCoroutine`) waited up to 20 s for `SensorSystemController.IsReady()`, which is gated solely on the ZED. With the camera DISABLED (FSR-only testing, zed=False) IsReady() never turns true, so it burned the full timeout before every preview. Both loops now run only `if (SessionContext.ZedEnabled)` — ZED off → preview immediately; ZED on → unchanged (exits when ready ~1-2 s, 20 s cap only when the camera is enabled-but-missing).
**Why:** A camera not in the session was holding up the live preview/pad-check for 20 s each time.
**Verify:** Unity Roslyn 0 errors. Effect applies on the next Unity rebuild (the running binary is still the July-24 build).

---

### 2026-08-21  14:05
**DO:** **Exercise-guide media upload (GIF/WebP + video).** Two gaps fixed: the Exercises library had no way to add a GIF (its only upload, "Import", is locked to zed_skeleton.json), and uploaded video showed a "not supported" placeholder. Added (1) a library **"Upload media"** button + `ExerciseRepository.importMedia()` (copies a gif/webp/mp4/mov/webm into the exercise folder as an `ExerciseSource.uploaded` asset), and (2) real desktop video via **media_kit** (video_player has no Windows support) — new shared `ExerciseMediaView` renders animated images via Image.file and video via media_kit (libmpv), looping+muted. Routed every guide surface through it: `AvatarExerciseGuide` now branches recorded→skeleton avatar vs uploaded→media clip; the protocol-builder per-block reference animation (`_guidePlayer`/`_refAnimation`) now plays video instead of the placeholder. `MediaKit.ensureInitialized()` added to main().
**Why:** User couldn't upload a GIF as an exercise guide — the feature was half-built (model + display for "uploaded media" existed, the create-UI and video rendering didn't).
**Verify:** `flutter analyze` clean; dashboard rebuilt with media_kit and launched (pid 53572); `libmpv-2.dll` + media_kit plugin DLLs confirmed bundled in the runner output, so runtime video works. media_kit deps added to pubspec (media_kit ^1.1.10 / media_kit_video ^1.2.4 / media_kit_libs_windows_video ^1.0.9); `flutter pub get` OK.

---

### 2026-08-24  11:35
**DO:** **User-testing prep.** (1) Fixed the left-rail overflow on the home screen (`_LeftRail`): the 7 nav cards now live in an `Expanded`+`SingleChildScrollView` and the Unity-status/version footer is pinned below, so the rail scrolls instead of overflowing at short window heights (was a debug-only yellow/black striped bar). (2) Built a **Release** Windows dashboard (`flutter build windows --release`) — no debug banner, no overflow stripe, faster — libmpv + media_kit plugins confirmed bundled. Launched Release dashboard (pid) + relaunched the rebuilt Unity engine; WS bridge on 8765 confirmed listening.
**Why:** User testing today — needed a clean, fast build with no visible layout glitch, and both halves of the system running.
**Verify:** `flutter analyze` clean on home_screen; `√ Built …Release\telehealth_dashboard.exe`; both processes running, port 8765 listening. Reminder for testing: FSR=COM3, EEG=COM7 in Settings.

---

### 2026-08-25  10:30
**DO:** **Improve ZED capture of adduction (limb-against-torso self-occlusion).** In `ZedController.OnZEDReady`, before `StartBodyTracking()`, force `bodyTrackingModel = HUMAN_BODY_ACCURATE` (was MEDIUM), `bodyTrackingPredictionTimeout = 1.0s` (was 0.2s), and `enableBodyFitting = true`. Overrides the scene/Inspector values in code so it's reliable. bodyFormat stays BODY_38.
**Why:** During adduction the arm/hand comes against the torso; the MEDIUM model drops the wrist/elbow to low confidence/NaN and the 0.2s prediction timeout blanks the joint out. ACCURATE tracks occluded limbs far better; longer prediction keeps the joint alive through the hidden part of the stroke; body fitting infers hidden joints from the kinematic skeleton. (User feedback after day-1 testing, 6 users.)
**Verify:** Unity Roslyn 0 errors; `HUMAN_BODY_ACCURATE` confirmed valid in the plugin (pairs with HUMAN_BODY_38_ACCURATE AI model). NOTE: needs a Unity rebuild to take effect; first launch does a one-time ACCURATE-model optimization (~minutes) + is more GPU-heavy — do the rebuild between participants. Today's mid-session mitigations: small arm-torso gap, face the camera, slight front-¾/raised camera angle.

---

### 2026-08-26  11:51
**DO:** **Deployed the ZED adduction fix.** Rebuilt the Unity engine (headless batchmode) so the 2026-08-25 change (HUMAN_BODY_ACCURATE + 1.0s prediction timeout + body fitting) is now in the running `Smart Game.exe`; verified the fresh Assembly-CSharp.dll (0 compile errors) and relaunched engine + Release dashboard for day-3 testing.
**Why:** The 08-25 tracking change was source-only until a rebuild; day-3 session needed it live.
**Verify:** DLL compiled after source edit, 0 errors; WS 8765 listening. First real ZED session carries the one-time ACCURATE-model optimization (~minutes, then cached).

### 2026-09-10  (user-testing log)
**DO:** **User-testing progress + operational note.** Recurring launches of engine + Release dashboard for testing sessions (both run detached; ports FSR=COM3 / EEG=COM7). Participant counts as reported: day 1 = 6, day 3 = 10, testing ongoing.
**Why:** Track testing milestones alongside code changes, per request to keep the log current for everything new — not only code diffs.
**Verify:** No functional/code change in these launches; system confirmed up (engine + dashboard running, 8765 listening) at each start. Only issue surfaced in testing so far = the adduction/ZED occlusion (fixed + deployed above).
